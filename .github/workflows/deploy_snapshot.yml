name: Deploy snapshot with Maven (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Snapshot deployment workflow is disabled for this fork. Use the Build workflow for CI artifacts."
