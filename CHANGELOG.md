# 更新记录

本文档记录 SimpMC-BedWars 2.x 系列面向服务器管理员和玩家的变化。

## 版本规则

从 `2.10.0` 之后，版本格式固定为 `2.10.y`：

- `2.10`：固定版本前缀，不再因新功能或 BUG 修复修改。
- `y`：每次发布新 JAR 时递增 1，不区分新功能、BUG 修复或性能优化。
- 仅修改文档、测试或开发流程且不发布新 JAR 时，不修改插件版本号。
- 已经发布的历史版本不回溯改号，避免同一版本号对应多个不同 JAR。

示例：

- `2.10.0` 的下一次发布为 `2.10.1`。
- `2.10.1` 之后无论加入功能还是修复 BUG，下一次发布均为 `2.10.2`。

## 尚未发布

### 文档

- 增加本更新记录。
- 增加 BUG 记录和报告模板。
- 明确 `2.x.y` 版本号递增规则。

## 2.10.1 - 2026-07-18

### 行为调整

- 游戏通过开局校验后，直接删除玩家当前背包、装备栏及末影箱中的全部游戏内物品，再发放本局默认装备和物品。
- 聊天名称与消息之间的白色分隔符由 `>>` 改为 `>`，分隔符之后的默认消息颜色由白色改为灰色 `&7`。
- 运行时会把旧语言配置中的冒号、`>>` 或旧颜色格式统一规范为新的 `&f> &7{message}`，无需管理员手动修改语言文件。

### 版本管理

- 从本版本开始，仅递增版本号最后一位。

## 2.10.0 - 2026-07-18

### 新功能

- 大厅玩家不能移动或拖拽物品、给自己装备物品、交换主副手、使用或消耗原版物品以及进行合成；管理员显式进入大厅建造模式后不受此限制。
- 每次游戏通过双队校验并正式开局时重新清空游戏内末影箱，等待阶段放入的物品不会带入本局；MULTIARENA/SHARED 模式仍会在离场时恢复玩家进入竞技场前的末影箱。

### BUG 修复

- 语言文件扫描现在只接受严格的 `messages_<语言>.yml` 文件，不再把 `messages_zh_cn.yml.v1.bak` 等自动迁移备份加载成 `zh_cn.v1.bak`，从而消除重复的缺失语言键报错。

### 性能与实现

- 大厅保护直接复用 Bukkit 事件，不增加轮询任务。
- 末影箱只在成功开局时清空一次，不在倒计时或队伍校验失败时重复操作。

## 2.9.0 - 2026-07-18

### 新功能

- 大厅入口状态统一：玩家每次进入大厅都会切换为冒险模式；OP 进入后仍可通过指令主动修改，下一次进入大厅时再次恢复冒险模式。
- 竞技场等待点、观战点以及队伍出生/复活点会单独保存玩家的 `yaw,pitch`，传送后保留设置时的朝向；位置坐标仍使用方块中心格式。
- 竞技场配置自动升级到版本 6，并迁移旧位置中的朝向字段；缺失的旧朝向使用 `0.0,0.0`。
- 开局分队强制产生至少两支非空队伍；仅当完整保留邀请组会造成单队游戏时，才拆分一名玩家到对手队伍。

### BUG 修复

- 修复大厅状态流程把玩家设置为生存模式的问题。
- 修复 `minPlayers` 小于 2 或满员邀请组恰好占满一队时，游戏可能以单支队伍开始的问题。

### 性能与实现

- 大厅模式只在实际进入大厅时设置，不增加循环检查；语言切换等物品刷新不会反复修改玩家模式。
- 分队结果先完整校验，再创建队伍成员和床全息图，避免失败开局留下部分运行时状态。

## 2.8.0 - 2026-07-17

### 新功能

- 区分普通死亡和虚空死亡：普通死亡在原地进入旁观，虚空死亡回到队伍安全出生点。
- 玩家主动进入虚空且近期没有有效攻击者时，显示“自走虚空”。
- 玩家聊天统一使用白色 `>>` 分隔玩家名称和消息。
- 自动迁移简体中文的旧聊天格式和虚空死亡提示。

### BUG 修复

- 修复竞技场销毁后，侧边栏和奖励定时任务继续读取已清空数据而持续产生 NPE 的问题。
- 修复竞技场销毁时部分定时任务、记分板和生成器没有完整注销的问题。
- 删除死亡旁观的重复模式切换和冻结回退逻辑。

### 性能

- 删除死亡时面向全场玩家的隐藏操作。
- 合并复活时重复的玩家显示和出生同步，只保留一次必要同步。
- 删除不再使用的旧复活数据包补丁代码。

## 2.7.0 - 2026-07-17

### 新功能

- 禁止授予原版进度和成就。
- 商店及升级村民单独保存朝向，旧竞技场自动计算并迁移朝向。
- 大厅自动补齐历史战绩、竞技场选择和离开三件物品。

### BUG 修复

- 修复死亡旁观模式的调用顺序。
- 修复从竞技场或设置模式返回大厅后游戏模式和物品不正确的问题。
- 加强大厅 PVP 保护。

## 2.6.0 - 2026-07-17

### 新功能

- 增加开局前组队邀请、接受、离开及队伍查看命令。
- 未组队玩家按单人队处理；队伍不足时自动随机分配剩余玩家。
- 增加独立的队伍分配规划器及测试。

## 2.5.0 - 2026-07-17

### BUG 修复

- 改进死亡旁观和复活过程。
- 修复从竞技场设置模式返回大厅时的传送、物品和模式恢复。
- 修复商店、升级 NPC 及床显示生命值的问题。
- 加强原版生物生成限制。

## 2.4.0 - 2026-07-17

### 新功能

- 增加 `/bw listTeams`，列出当前竞技场的所有队伍。
- 增加安全出生点搜索，支持一格高空间并使用趴下姿态。
- 在玩家列表中使用特殊颜色标记队友。
- 合并 Spawn 和 Respawn 配置，移除重复位置声明。

## 2.3.0 - 2026-07-17

### 新功能

- 将出生点和复活点的处理分离，为后续安全出生逻辑提供基础。
- 增加断线 30 秒后自动视为离开的处理。

### BUG 修复

- 修复部分玩家不能使用 `/shout` 的问题。
- 修复旧位置格式在新 Paper 版本上的解析问题。

## 2.2.0 - 2026-07-16

### 新功能

- 为主要配置文件增加中文注释。
- 将面向玩家和管理员的硬编码提示汉化。
- 增加配置项注释写入能力。

## 2.1.1 - 2026-07-16

### BUG 修复

- 修复旧大厅坐标字段数量不一致导致的 `ArrayIndexOutOfBoundsException`。
- 自动迁移旧大厅坐标格式。
- 清理重复和失效的旧配置内容。

## 2.1.0 - 2026-07-16

### 新功能

- 建立自动配置版本迁移、备份及废弃配置清理机制。
- 新竞技场自动禁止昼夜变化和生物自然生成。
- 自动寻找各队伍的床。
- 竞技场标点统一保存为方块中心坐标。

### 性能

- 优化数据库、治疗池、竞技场载入和配置访问路径。
- 增加配置迁移及位置解析测试。
