# 更新记录

## 10.0.1 - 2026-08-17

### 复活判定修复

- 复活资格现在在玩家死亡瞬间固定：死亡时床仍存在的玩家会完成本次复活倒计时，即使倒计时期间床被破坏；死亡时床已毁则直接淘汰。
- 修复死亡到 `PlayerRespawnEvent` 之间约 3 tick 内床被破坏时，原本应复活的玩家被错误判为最终淘汰的问题；断线时会清理该局部状态，不新增配置项或周期任务。

## 10.0.0 - 2026-08-17

### 火球发射差异

- 站立发射的默认初速度从 1.6 轻微下调到 1.5 格/tick；潜行发射仍为 2.4 格/tick，因此潜行同时具有更高初速、双倍持续加速度和后坐，不再与站立发射一起提高。
- 潜行持续加速度保持每 tick 0.2，站立保持每 tick 0.1；本次不改变加速度、射程、后坐、冷却或爆炸参数。
- 主配置架构升级到 30，只将仍等于旧默认值的 `speed-multiplier: 16`、`sneak-speed-multiplier: 1.5` 更新为 15 和 1.6，不新增配置项，也不会覆盖管理员自定义速度。
- 此调整改变默认游戏行为，按版本规则发布 10.0.0。

## 9.0.4 - 2026-08-16

### 稳定性与性能

- 火球爆炸半径和击退强度现在在加载配置时限制为有限安全范围；异常的大数不会再造成大范围实体扫描、速度溢出或 Paper setVelocity 异常。
- TAB 周期权威刷新改为每 tick 分批处理查看者，避免多人场景在同一 tick 集中重建并发送所有玩家行，同时保留被其他插件覆盖后的客户端自愈能力。
- 不新增逐 tick 全量扫描；加入、淘汰、换队和重连仍通过现有事件路径立即刷新。


## 9.0.3 - 2026-08-16

### TAB 队伍前缀降级显示

- Paper 1.21.11 客户端未稳定采用 `PlayerInfo UPDATE_DISPLAY_NAME` 中的玩家名颜色时，竞技场 TAB 行现在仍会显示 `{teamColor}[{teamLetter}] ` 前缀；即使名称本身保持白色，也能通过可见队伍首字标识区分归属。
- 玩家行不再删除管理员语言模板中的 `{teamName}` 与 `{teamLetter}`，自定义队伍前缀可以正常解析。所有内置语言的进行中与结算队伍行使用紧凑前缀，旧内置空前缀会按语言配置架构迁移，自定义前后缀保持原值。

### 火球重合点击退

- 火球命中位置与玩家坐标完全重合时，不再对零长度向量执行归一化；该边界改为施加纯向上的有限击退，避免 Paper 在 `setVelocity` 抛出 `IllegalArgumentException: x not finite`。
- 水平和垂直击退强度在计算入口统一限制为非负有限数，异常配置值也不会生成 `NaN` 或无穷速度。
- 本次不改变公开 API、商品、竞技场格式或游戏规则，属于向后兼容的客户端显示降级与运行时异常修复，因此发布 9.0.3。

## 9.0.2 - 2026-08-14

### 召唤物敌方目标选择

- 蠹虫和铁傀儡现在统一按当前竞技场、队伍归属与玩家生命周期选择目标；同队玩家、死亡玩家、复活等待者、旁观者、跨世界玩家以及非进行中对局的玩家都不会进入候选。
- 原版生物尝试锁定友方或其他无效目标时，目标事件会立即改选最近的合法敌人，不再只取消友方目标后让蠹虫反复追逐自己人。生成时也会立即选敌，当前合法目标保持不变。
- 每秒兜底刷新不再无条件重新扫描和覆盖目标，仅在当前目标失效时搜索替代敌人；这保留铁傀儡主动找敌与玩家状态变化后的恢复能力，同时减少无意义的全量距离计算。
- 本次不改变公开 API、配置格式或商品行为，属于向后兼容的选敌错误修复，因此发布 9.0.2。

## 9.0.1 - 2026-08-14

### TAB 队伍颜色

- TAB 玩家名不再依赖客户端把空 `PlayerInfo displayName` 回退到 scoreboard Team。插件现在按查看者批量发送“前缀 + 明确队伍色玩家名 + 后缀”的完整显示名，玩家自己、同队、敌队、淘汰者和旁观者看到的名字都会稳定使用当前队伍颜色；scoreboard Team 继续独立负责头顶名牌颜色、碰撞与隐身可见性。
- 显示名缓存改为记录完整渲染文本，前缀、后缀、动画帧或队伍颜色变化时会立即更新，未变化行不会重复发包；强制重放仍能修复 Paper `ADD_PLAYER`、跨世界加载或其他插件后写造成的覆盖。
- 加入、重连与淘汰刷新不再跳过目标玩家自己的 Sidebar，因此本人 TAB 行不会保留白色或旧队伍色；本人观察者行仍只发送显式名称，不会收到伪造的 `UPDATE_GAME_MODE: SPECTATOR`，避免再次引入客户端移动与服务端模式不同步。
- 本次只修复客户端显示和刷新时序，不改变公开 API、配置格式或对局规则，因此按向后兼容错误修复发布 9.0.1。

## 9.0.0 - 2026-08-14

### 隐身装备同步与队伍升级价格

- 隐身现在会向敌队和旁观者同时隐藏主手、副手、头盔、胸甲、护腿和靴子；同队继续看到真实装备。切换快捷栏、交换副手、拾取、丢弃、购买或更换装备时，Paper 装备变化事件会在下一 tick 合并重发一次六槽隐藏状态，不增加周期扫描。
- 敌方玩家、投射物和玩家点燃的 TNT 只有在伤害事件最终未取消且实际伤害大于零时才解除隐身；自伤、友伤、复活保护或被其他插件取消的攻击不会误解除。自然到期、报警陷阱、死亡复活、离场和结算统一清理药水、计时、名牌与装备显示，不再出现药效已消失但装备仍被隐藏的状态。
- 锋利 I–IV 默认价格调整为 2、4、8、16 钻石；保护 I–IV 默认价格调整为 2、4、10、24 钻石。`upgrades2.yml` 架构升级到 10，只迁移仍精确等于旧内置整组值的类别；锋利与保护分别判断，任一级自定义都会保留该类别的四级整套价格。
- 默认对局经济价格会发生变化，因此按不兼容游戏行为变更发布 9.0.0。

## 8.1.0 - 2026-08-13

### 回城卷轴

- 实用道具商店新增回城卷轴，默认价格为 3 个钻石；右键使用后进行 5 秒引导，完成后传送到本队出生点的安全位置。
- 引导期间每秒单独向使用者发送倒计时 ActionBar；玩家死亡、重生、淘汰、离开竞技场、游戏结束或队伍出生点失效时自动取消，不能叠加新的回城引导。
- 引导期间每秒只向同一竞技场内、距离使用者 24 格以内的其他玩家发送 8 个蓝色粒子；粒子不逐 tick 生成、不向全服广播，降低低配客户端和服务器负担。
- 旧商店和语言配置会自动补入回城卷轴商品与中文文本；若默认槽位 32 已被自定义商品占用，会改用下一个空闲内容槽位，管理员已有商品、语言和价格自定义值不会被覆盖。

## 8.0.1 - 2026-08-08

### 精简玩家物品指南

- 将玩家说明重写为简洁的物品指南，集中介绍资源与商店操作、基础装备、特殊道具、团队箱子、死亡保留规则以及大厅和旁观物品。
- 按当前源码和内置简体中文商品说明核对火球、TNT、搭桥蛋、蠹虫、梦境守卫者、魔法牛奶与袖珍弹出塔的操作方式；删除射程、速度等不适合玩家快速阅读的实现参数，并明确服务器可自定义的内容以游戏内说明为准。

## 8.0.0 - 2026-08-08

### 潜行火球持续加速、严格反向后坐与射速提升

- 修复 Paper 1.21.11 `Fireball#setAcceleration(Vector)` 实际调用 `assignDirectionalMovement`、会把刚设置的 1.6/2.4 格/tick 初速度覆盖为 0.1 的问题。版本支持层现在直接写入服务端 `accelerationPower`，不再改动当前速度；普通火球保持 1.6 初速度与 0.1 持续加速度，潜行火球保留 2.4 初速度并通过新增 `sneak-acceleration-multiplier: 2.0` 使用 0.2 持续加速度。
- 潜行后坐默认值由 0.05 提高到 0.10，硬上限由 0.08 提高到 0.20；方向从仅水平反推改为火球发射速度的完整三维反方向，向上或向下发射时会包含对应的垂直反作用分量。
- 火球默认冷却由 0.5 秒缩短到 0.4 秒，持续射速约每秒 2.5 发，一个 1 秒窗口内通常可发 2 至 3 个；边界判断改为精确允许满 400ms 时发射。200–300 格随机最大射程及逐 tick 实际路径累计保持不变。
- 主配置架构升级到 29：新增加速度倍率，并只把仍为旧内置值的后坐力和冷却迁移到新默认值；管理员自定义值保持不变。新增纯物理、配置迁移、NMS 字段写入测试和固定 Paper 1.21.11 运行探针。由于默认火球速度、后坐和射速都会改变对局行为，本次按不兼容游戏行为变更发布 8.0.0。

## 7.1.1 - 2026-08-06

### 结算侧边栏、返回物品与观战模式同步

- 修复结算排行榜上下文错误依赖“游戏结束聊天排行榜”的问题。关闭聊天排行榜、把其文字配置为空或走无唯一胜者的超时结算时，侧边栏现在都会在进入 `restarting` 时自行建立一次统计快照；`{topPlayerDisplayName}`、`{topTeamColor}`、`{topTeamName}`、`{topValue}` 与胜者队伍占位符不再原样显示。离开结算状态后立即释放快照，下一局不会复用旧数据；无胜者时使用语言文件中的“无玩家”并清空队伍颜色和首字母。
- 结算状态入口现在统一收口全部在线参赛者和旁观者：先结束尚未完成的死亡重生会话，退出第一人称或死亡 `SPECTATOR`，恢复为可交互的 `ADVENTURE`，再按参赛者/旁观者分别设置飞行能力并发放旁观物品。该处理位于侧边栏和 TAB 重建之前，覆盖正常胜利、无胜者超时和无人存活路径；获胜者不会再在返回物品发放后被旧结束监听器二次清空背包。
- 游戏结束掉落物清理不再因为活跃玩家列表为空而提前返回；无存活玩家的比赛也会清理地图中的掉落物。
- 修复 TAB 观察者样式把 `UPDATE_GAME_MODE: SPECTATOR` 发给查看者自己、导致 1.21.11 客户端把本地玩家切入旁观移动逻辑，而 Paper 仍认为其处于 `ADVENTURE` 的问题。本人 PlayerInfo 现在始终使用服务器真实模式，旧缓存会立即恢复；其他玩家看到的 TAB 观察者样式、队伍色、同队聚合和排序保持不变。
- 删除重连成功后另一个延迟任务对 `SURVIVAL + flying` 的抢写。死亡等待与复活继续由竞技场重生会话唯一管理，普通观战保持 BedWars1058 的 `ADVENTURE + 飞行` 风格，第一人称观战及死亡等待所需的真实模式只通过 Bukkit `Player#setGameMode` 同步切换。没有新增定时扫描、配置项或不兼容 API 变更，因此发布为 7.1.1。

## 7.1.0 - 2026-08-04

### 方块所有权、踢出结算与 TAB 观察者显示

- 修复玩家放置方块位于普通地图保护区或可建造区边界时，会被保护区优先判定为原地图方块，从而无法徒手破坏或被爆炸摧毁的问题。对局内已确认的玩家方块现在始终优先于地图保护；原地图、队伍床和 NPC/生成器保护仍按原规则生效，开启 `allowMapBreak` 也不会绕过这些专用保护。
- 玩家方块所有权不再在方块破坏、爆炸、燃烧、消退、物理销毁或下落方块事件中提前删除。监听器会保存事件前快照，并在下一 tick 只读取仍已加载的目标区块，以实际世界结果决定保留或移除所有权；被后续监听器取消、被其他插件恢复或最终没有发生的破坏不会把仍存在的方块永久降级为地图方块。单方块、多方块放置、火球/TNT 爆炸、Paper 方块连锁破坏和沙砾等下落方块使用同一套校正规则，不增加全场扫描或强制区块加载。
- 爆炸射线同样改为先读取玩家方块所有权：玩家搭建的方块不会仅因坐标落在地图保护区内就变成防爆方块；队伍床仍始终受保护。所有权与破坏判定均有独立纯策略回归测试，覆盖保护区、禁止破坏地图、事件取消、世界状态未变化和多人放置边界。
- 修复玩家重连后再次被反作弊封禁或服务器踢出时，旧 `leaving` 防重状态按 UUID 命中新 Player、导致退出清理直接返回，离线玩家永久残留在队伍并使最后一名对手无法获胜的问题。成功重连会在恢复竞技场成员关系前按 UUID 结束旧离场生命周期，后续退出能够再次完整清理玩家、队伍和竞技场映射。
- Paper 1.21.11 的 `PlayerQuitEvent.QuitReason.KICKED` 现在统一视为放弃比赛：无需绑定特定反作弊 API，实际踢出会先走正常离场，再立即取消重连占位并重新检查胜负。普通 `DISCONNECTED` 和 `TIMED_OUT` 仍保留 `rejoin-time` 重连窗口；多人队只移除被踢玩家，仍有在线队友或其他尚在有效重连窗口内的掉线队友时不会错误淘汰整队，也不会重复触发结束事件。
- TAB 玩家行不再显示队伍名称或队伍首字母，只保留 scoreboard Team 提供的队伍颜色、同队聚合、队内字典序和光谱队伍顺序；头顶名牌继续复用同一队伍色来源。最终淘汰者和纯旁观者会按每个查看者发送 `UPDATE_GAME_MODE: SPECTATOR`，只改变 TAB 行的观察者样式，不修改服务端真实 `GameMode`、飞行、观战菜单或交互状态；离开 BedWars 上下文时会从 Paper 当前状态恢复真实模式和第三方 nullable 显示名。
- 内置语言中的游戏中/重置阶段玩家行默认前缀移除 `{teamName}`、`{teamLetter}` 及对应胜利星标队名，淘汰后缀也不再重复队名。简体中文语言架构升级到 16，其他内置语言升级到 6；迁移器只替换完全等于历史内置值的已知 TAB 前后缀，管理员自定义模板、游戏时间文字和未知语言键在磁盘上保持不变，但玩家行运行时会把 `{teamName}`、`{teamLetter}` 解析为空以保证只显示队伍色。新增 PlayerInfo 游戏模式数据包构造与恢复探针，和现有 nullable 名称探针共同验证 Paper 1.21.11 的真实运行时契约。
- 本次新增按查看者设置 TAB 观察者模式的公开 `PlayerTab.PlayerListMode` 能力，并保持旧构造入口兼容；按向后兼容的新功能发布 7.1.0。

## 7.0.0 - 2026-08-03

### 无冷却喊话与 TAB 游戏时间

- `/shout <消息>` 与 `!消息` 不再有插件冷却；只保留现有 `bw.shout`/`bw.command.shout` 权限检查。旧的 UUID 到期时间表和 `bw.shout.bypass` 权限已删除，主配置架构升级到 28 并移除 `shout-cmd-cooldown`，无论该值是否由管理员自定义都不再影响喊话。
- `%bw1058_player_team%` 的喊话标记不再借用冷却表。现在仅在当前聊天事件嵌套解析 PlaceholderAPI 格式时建立线程隔离上下文，并通过 `finally` 恢复；异步聊天、占位符异常和并发玩家都不会留下过期状态。
- 游戏进行中的存活玩家、已淘汰玩家和纯旁观者现在都会在 TAB 页首看到本局已进行时间。新增 `{gameTime}`，格式为 `MM:SS`，超过一小时后为 `HH:MM:SS`；现有 `{time}` 继续表示开局、重置或下一事件倒计时，不改变旧模板语义。
- 游戏时间直接读取竞技场已有的 `startTime`，复用默认每 20 tick 的 TAB 页首页尾刷新和内容缓存；没有新增计时任务、逐 tick 逻辑或全服玩家扫描。占位符渲染也不再求值当前文本根本没有引用的供应器，避免每一行重复计算游戏时间和其他动态值。语言键 `format-tab.playing.game-time` 可自定义或留空隐藏；自定义语言架构升级到 4、内置语言升级到 5、简体中文升级到 15，已有自定义文字保持不变。
- 修复 `%bw1058_elapsed_time%` 把总分钟和总秒直接拼接、导致 65 秒显示为 `01:65` 的问题。TAB 与 PlaceholderAPI 现在使用同一纯格式化实现，未来开始时间安全钳制为 `00:00`。
- 删除喊话冷却会改变既有游戏行为和主配置格式，因此按仓库语义化版本规则发布 7.0.0；已弃用的 `ConfigPath.GENERAL_CONFIGURATION_SHOUT_COOLDOWN` 常量仅为附属插件二进制兼容和旧配置迁移保留。

## 6.0.0 - 2026-08-03

### 所有游戏世界强制保持正午与最高自然天光

- 修复 Paper 1.21.11 将 `doDaylightCycle`、`doWeatherCycle` 和 `doMobSpawning` 的注册键分别改为 `advance_time`、`advance_weather` 和 `spawn_mobs` 后，旧字符串查找静默失败的问题。插件不变量现在直接使用 1.21.11 的现代 `GameRules` 常量；竞技场文件中的旧 Bukkit 规则名继续通过显式别名解析，不需要管理员改写现有配置。
- `MULTIARENA` 与 `BUNGEE` 专用实例中的所有已加载、新建和恢复世界都会固定为原版 `/time set noon` 对应的 `6000 tick` 并保持晴天；`SHARED` 只处理已启用竞技场、待加载竞技场、设置世界和 `/bw setLobby` 明确配置的 BedWars 大厅，不影响同服其他玩法世界。运行中重新设置大厅会立即应用，不必等待重启或世界重载。
- `advance_time` 与 `advance_weather` 在初始化时关闭；之后 `/time`、睡眠、插件跳时和 `/gamerule` 修改会在事件末端被改回正午/关闭状态，雨雪和雷暴也无法重新开始。处理完全由启动、世界生命周期及状态变化事件驱动，不增加逐 tick 或周期性世界扫描。
- `6000 tick` 加晴天保证普通主世界露天的最高自然天空亮度；它不会把洞穴、屋内、下界或末地变成客户端 Fullbright。已经下雨的世界按原版规则最多需要约 100 tick 完成视觉淡出。
- 自动发布流程增加固定 Paper 1.21.11 build 132 的游戏规则注册表运行探针，和现有 TAB 数据包探针一起验证真实补丁服务器契约。本次会改变专用 BedWars 实例的世界环境，按不兼容游戏行为变更发布 6.0.0；没有新增配置键或配置架构迁移。

## 5.0.0 - 2026-08-03

### 火球射程、击杀归属与锋利价格

- 火球每次发射都会在 `fireball.flight-range.min: 200` 与 `max: 300` 之间独立抽取一次最大飞行距离，并按相邻 tick 的实际移动量累计路程；达到本次上限后立即移除。跟踪任务绑定火球实体，不扫描全服实体，也不强制加载远处区块；提前撞到方块、实体、世界边界或未加载区块时仍会正常提前结束。
- 修复火球使用无来源自定义伤害时，直接炸死玩家只显示未知死因或不显示发射者的问题。敌方伤害现在使用 direct 为火球、causing 为发射者的 Paper 1.21.11 玩家爆炸 `DamageSource`，死亡解析优先读取当前伤害源；火球击落虚空则继续使用原子更新的最新一击。只有插件同步定额结算窗口内的火球/目标组合会放行，原生接触与原生爆炸实体伤害继续取消，避免双倍伤害；自伤和队伤不会把发射者或队友错误登记为击杀者。
- 队伍锋利 I–IV 的默认价格降为 2、4、8、14 钻石。`upgrades2.yml` 架构升级到 9：架构 8 只在四级整组仍为上一版内置价格 4、6、9、14 时迁移，更早架构只迁移整组原内置价格 4、8、16、32；只要任一级被自定义，整组价格都保持不变。
- 主配置架构升级到 27 并按模板顺序补入火球射程上下限。由于默认火球射程和队伍升级价格会改变实际对局平衡，本次按不兼容游戏行为变更发布 5.0.0。

## 4.0.8 - 2026-08-02

### 恢复并固定大厅代理返回物品

- 修复旧配置架构 15 曾无条件删除管理员自定义 `lobby-items.leave`、架构 18 又补成默认红床的问题。主配置架构升级到 26：若当前返回项仍是被补入的内置默认值，会从架构 15 删除前的最后快照，或架构 15–17 中重新配置过的最新快照，一次性恢复完整自定义节点；管理员后来重新配置或删除的值不会被覆盖。
- 配置迁移不再于每次架构升级无条件创建 `stats`、`arena-selector` 和 `leave`。已有节点仍会补齐缺失字段，管理员删除整个节点后保持删除；仅架构 15–17 的已知破坏窗口会在没有等价返回项、且默认槽位未被占用时补回默认项。
- 正常大厅流程移除固定 15 tick 的全背包清空。竞技场传送完成后在主线程立即替换旧 BedWars 命令物品，并在 15 tick 后做一次带实时上下文校验的选择性复核，既防止登录链后续事件替换返回物品，也不会删除其他插件持有的非 BedWars 物品。公开兼容方法 `sendLobbyCommandItems` 仍保留原有“延迟后清空背包再发放”的契约；`applyLobbyState` 的异步调用会安全转交主线程。
- 所有大厅物品写入统一校验在线状态、竞技场映射、设置会话和真实大厅世界。玩家经传送门或附属插件跨世界进入大厅也会应用同一状态，不会继续持有等待区的本服返回物品；旧上下文不能在玩家进入新竞技场后清空新背包。
- 大厅物品改为逐项解析。单个无效材质、缺失字段或越界槽位只输出一次中文警告并跳过自身；重复槽位不再静默依赖 YAML 顺序，`leave` 节点和兼容的 `bw leave` 定义具有明确优先级，确保代理返回入口不被普通物品替换。

## 4.0.7 - 2026-08-02

### 消除死亡与复活时的集中卡顿

- 最终淘汰不再于队伍成员尚未移除时同步重建全场侧边栏。淘汰后的刷新改为下一 tick 按竞技场合并；同批只计算一次占位符，每名仍在线且仍在原竞技场观战的淘汰玩家只重放一次 TAB 行，并避免可见性恢复重复重放同一行。期间已离线、离场或换场的玩家不会被延迟任务重新加入旧 TAB。
- 侧边栏占位符刷新改为增量写入。标题、行文本或计分文字没有变化时不发送更新；仅在 objective 或行 Team 结构缺失时完整重建，保留原有 TAB 和头顶队伍颜色渲染语义。
- 复活附魔恢复不再于每个背包槽位重复执行完整背包同步。弓、剑和护甲处理完成后，仅在物品实际变化时统一同步一次。
- 安全出生点搜索只读取当前 tick 已加载的区块；加入告示牌注册不再读取方块类型，并按世界和区块建立索引。告示牌及墙后支撑块均已加载时才写入，任一相关区块实际加载后按事件补写最新文字和状态，避免死亡或竞技场加载路径意外触发区域文件读取。
- 最终淘汰的加入告示牌与 Citizens 人数显示移出观战状态数据包批次，并合并同 tick 的重复更新，降低多人同时淘汰时的单 tick 峰值。
- 日志中的 `Corrupt regionfile header` 表示地图 `.mca` 文件已经损坏；本版会避免可选扫描额外读取未加载区块，但无法恢复已经丢失的区块数据，仍须使用完好地图备份替换。

## 4.0.6 - 2026-08-02

### 补齐最小 TAB 模式与跨世界同步

- 移除玩家列表排序对完整 TAB 前后缀配置的错误依赖。即使 waiting、starting、playing 或 restarting 的完整格式关闭，竞技场玩家仍按光谱队伍顺序聚合，同队内按玩家名字典序排列。
- 排序收集逻辑集中为独立入口，并覆盖默认关闭 waiting/starting 完整格式时仍必须纳入竞技场玩家的回归场景。
- SHARED 模式普通跨世界不再等待 Paper 1.21.11 不会触发的二次客户端加载事件：`PlayerChangedWorldEvent` 下一 tick 会主动依据当前世界重建 TAB 行并重新绑定 scoreboard；若登录/重生加载事件已经先完成则自动去重。从非大厅世界进入配置大厅不再继续显示原版列表，离开大厅也不会残留旧大厅行。

## 4.0.5 - 2026-08-02

### 恢复 TAB 队伍颜色的真实渲染链路

- 撤销 4.0.3/4.0.4 写入非空 PlayerInfo 显示名的错误方案。受管玩家行现在保持该字段为空，让 1.21.11 客户端统一使用 scoreboard Team 的颜色、前缀、后缀和成员关系；TAB 与头顶名牌重新共用 BedWars1058 的同一渲染来源。
- 等待和倒计时阶段不再只查询尚未分配的正式队伍：GUI 或 `/bw team` 保存的预选队伍会立即用于 TAB 颜色、同队聚合和光谱顺序。选择或取消时按事件刷新同一竞技场所有查看者中的整个小队，不等待周期任务。
- 竞技场队伍色上下文不再依赖右侧计分板、页首页尾或完整 TAB 前后缀开关；即使这些外观项全部关闭，插件仍建立最小 scoreboard Team，保证 TAB 和头顶名牌保留队伍颜色。
- 名称清空只发送给当前 BedWars 查看者，不修改目标玩家的全局 `playerListName`。离开上下文时从 Paper 当前状态恢复该查看者看到的名称；登录、跨世界和 `showPlayer` 造成的 `ADD_PLAYER` 重建则在对应事件后立即重放空值，避免用全局状态争抢其他插件的所有权。
- TAB 更新和周期重放前会确认玩家实际绑定的 scoreboard 仍是 BedWars 管理实例；若被其他插件替换，则把外部面板保存为退出恢复目标并重新绑定完整 BedWars 快照，不再只更新客户端看不到的旧对象。
- 旧竞技场延迟执行的回大厅任务会先核对玩家仍在线且尚未进入其他竞技场；玩家快速从 A 场切换到 B 场时，A 场回调不再覆盖 B 场已经建立的 TAB、头顶名牌和可见性状态。`giveSidebar` 入口也统一以实时竞技场注册表为准。
- 缓存只记录“该查看者的名称字段已经清空”，动画仅更新 scoreboard Team，不再重复发送内容不变的 PlayerInfo 包；登录、跨世界、`showPlayer`、选队和游戏状态变化仍会即时重放必要状态。

## 4.0.4 - 2026-08-02

### 收紧 TAB 与头顶名牌生命周期

- 多个 Sidebar 先后接管同一观察者时改为后进先出的所有权链：新实例接管前先恢复旧实例管理的 PlayerInfo 名称，释放后立即重放上一层完整状态。非顺序移除或重新挂载中间层时也会修复 scoreboard 回退链，不会恢复已经失效的旧面板。
- 同一 TAB 标识符改给另一名玩家时，先从每个观察者的 scoreboard team 删除旧成员，再加入新成员；旧玩家的头顶名牌不再继承新玩家的颜色、前后缀或可见性。
- `removeTab` 和 `removeTabs` 会释放不再使用的内部队伍名映射；批量清理后同时重置分配器，长期在线的大厅不再按历史玩家数量保留 UUID 和队伍名字符串。
- PlayerInfo 恢复包发送失败时保留按观察者去重的待恢复目标，下一次明确 TAB 刷新先重试恢复，成功后才释放缓存；目标已经离线或新状态已成功覆盖时立即清理，不增加逐 tick 扫描。
- 启动兼容检查现在会实际构造并检查带红色名称的 `UPDATE_DISPLAY_NAME` Entry 和数据包。发布工作流还会下载并校验固定 SHA-256 的 Paper 1.21.11 build 132，使用补丁后服务端类路径运行真实桥接探针，不再只依赖假的渲染器单元测试。

## 4.0.3 - 2026-08-01

### 直接修复 Paper 1.21.11 TAB 玩家名颜色

- 按 1.21.11 客户端的真实渲染优先级修复：非空 `displayName` 会完全跳过 scoreboard team，因此不再依赖客户端间接着色。插件现在针对每个查看者批量发送“前缀 + 明确的队伍颜色 + 玩家名 + 后缀”，不会修改玩家的全局 `playerListName`。
- scoreboard team 继续独立负责头顶名牌颜色、成员关系和隐身可见性；TAB 与头顶名牌不再错误地共用一条客户端渲染假设。
- 修复登录时的两个丢包窗口：首次世界加载早于异步大厅初始化时立即建立 TAB 上下文；向已有玩家更新新加入者时等待服务端发送 `ADD_PLAYER` 后再发送显示名。
- Paper 的 `showPlayer` 路径重建单个 PlayerInfo 条目后，立即只重放该观察者看到的目标玩家行；MULTIARENA 重连则等待玩家重新加入原队伍后再更新其他观察者，不再先写入白名。周期刷新按观察者合并成一个批量包，既能自愈其他插件的后续覆盖，也避免逐玩家数据包爆发。
- 同一次刷新只求值一次动画前后缀，并把相同帧用于 scoreboard 与 PlayerInfo，观察者数量不再加速动画或让 TAB 与头顶文字错帧。单次发包失败不会写入成功缓存，后续事件仍会重试。
- 退出 BedWars TAB 上下文时，从服务器当前状态恢复目标玩家真实的全局 TAB 名称，保留其他插件后来写入的值；多个 Sidebar 接管同一观察者时，旧实例不能覆盖新实例。插件卸载会同步清理大厅及竞技场的名称、页首页尾、scoreboard 和列表顺序。Paper 1.21.11 数据包桥接在插件启动时一次性校验并缓存，失败会输出明确错误而不会中断登录流程。
- 玩家加入竞技场、离开竞技场或退出服务器时，会从所有观察者的已部署 TAB 行中清除旧上下文；离线玩家的逐观察者显示名缓存直接释放，不再让长期运行的大厅随历史玩家数量持续增加 TAB 队伍、对象引用和周期刷新开销。

## 4.0.2 - 2026-07-31

### 修复生产环境 TAB 初始化与队伍颜色

- 恢复迁移时遗漏的登录延迟语义：多竞技场模式会在异步大厅传送回调完成、玩家状态初始化完成后再创建 TAB；Paper 确认客户端完成首次世界加载时会重放完整状态，玩家刚进入服务器不再停留在原版 TAB。
- 进入竞技场、观战、离场及游戏状态切换后不再只修改服务端缓存中的既有队伍。插件会用完整渲染的新 scoreboard 替换旧实例，使客户端重新收到包含成员、前后缀、队伍颜色和名牌可见性的 CREATE 数据；TAB 玩家名与头顶名牌共用同一队伍色。
- 修正 Paper 1.21.11 `playerListName()` 永远返回有效组件、默认状态也不返回 `null` 的真实语义。默认玩家名只清理一次，不再在每个查看者和周期刷新中向全服重复广播名称更新；退出 BedWars 上下文时也不会把默认白名恢复成会绕过 scoreboard team 的显式组件。
- `PlayerGoods` 不再保存并通过旧字符串 API 恢复 TAB 名称，移除另一个会把默认玩家名写成显式白色组件的覆盖源；外部插件的真实自定义名称仍由唯一的 TAB 生命周期状态负责保存和恢复。
- TAB 名称所有权按玩家连接实例隔离，旧连接残留的观察者引用不会干扰同 UUID 玩家重连；完整重放也会识别其他插件后来接管的 scoreboard，并在离开 BedWars 时恢复最新外部面板。

## 4.0.1 - 2026-07-31

### 修复 TAB 与头顶名称的队伍颜色

- 撤销 3.0.1 中错误的全局自定义 `playerListName` 渲染。非空的自定义名称会让客户端绕过每个查看者的 scoreboard team，因此此前虽然服务端写入了颜色，生产客户端仍可能显示白色名称。
- BedWars TAB 上下文存活期间保持 `playerListName` 为空，恢复由每个查看者的 scoreboard team 统一渲染队伍前缀、玩家名、后缀和头顶名牌；完整 TAB 样式关闭时仍保留最小队伍色 team，不再退回白名。
- 如果其他插件在游戏期间写入自定义 TAB 名称，下一次现有 TAB 刷新会先记住该值再恢复 scoreboard 渲染；玩家离开全部 BedWars TAB 上下文后恢复最新外部名称，避免覆盖其他插件状态。

## 4.0.0 - 2026-07-31

### 恢复 BedWars1058 开局门槛

- 恢复竞技场级 `minPlayers`：总人数达到该值后进入倒计时，`maxInTeam` 只限制单队容量；配置架构升级到 22 并删除不再参与匹配的 `minInTeam`。新增 `/bw setMinPlayers`，旧 `/bw setMinInTeam` 仅作为会提示迁移的兼容别名。
- 资格检查、真实分队、`TeamAssignEvent` 后校验和倒计时归零校验共用“全场最低人数 + 至少两支非空队伍 + 单队容量”规则。完整小队不会为制造对手被拆散，超过容量的外部大队伍仍可拆分。
- 分配器继续实时计算而不使用配置表或历史状态表；未组队玩家尽量分散到更多配置队伍并保持均衡，真实选队冲突或把所有人挤到同一队时自动回退为合法分配。
- `IArena` 新增默认返回 2 的 `getMinPlayers()`；`getMinInTeam()` 标记弃用并固定作为返回 1 的兼容桥。由于正常开局行为发生不兼容变化，本次按规则发布 4.0.0。

### TAB、大厅提示与地图保存

- 大厅、等待、倒计时、游戏、淘汰和结算 TAB 页首统一保证至少 128 空格宽度；旧 BedWars1058 的 104 空格页首及较窄自定义页首在发送时拓宽，更宽自定义值保持不变。
- 玩家从大厅断开时，会在任何竞技场、数据库或附属清理前向仍在大厅的玩家发送退出提示；从竞技场、设置世界或其他世界断开不会泄漏到大厅。
- 设置会话结束后，内部地图恢复器先等待玩家传出并确认世界成功卸载，再异步压缩地图；60 秒内无法安全卸载时放弃本次压缩，避免读取仍加载的 `.mca` 区域文件。缓存 ZIP 先写临时文件再原子替换，进程中断不会覆盖上一份完整缓存；BUNGEE 设置会话结束时会主动返回代理大厅。
- 补充 `Corrupt regionfile header` 排错说明：Paper 的日志表示地图数据已损坏，现有损坏区块仍需从完好原图恢复，并同步处理内部恢复缓存 ZIP。

### 验证

- 新增全场最低人数、容量、单队调试、小队边界、选队回退、最大分散、全部状态 TAB 宽度、退出提示范围、配置迁移和 API 默认值回归测试。

## 3.0.1 - 2026-07-31

### TAB 与头顶队伍颜色

- 不再依赖客户端把 scoreboard 队伍颜色套到默认玩家名：插件现在显式生成“队伍前缀 + 队伍色玩家名 + 后缀”的完整 TAB 组件，确保玩家名字本身使用所属队伍颜色，同时保留 BedWars1058 的状态化前后缀。
- 每个查看者的 scoreboard team 继续独立设置队伍颜色、成员和名牌可见性，因此玩家头顶名称会使用同一队伍颜色，隐身玩家仍隐藏名牌。
- 完整 TAB 样式被管理员关闭时，竞技场内仍保留最小的队伍色玩家名和头顶名牌，不再把核心队伍识别能力与可选前后缀配置一起删除。
- 增加完整 TAB 组件、多个查看者引用恢复、第三方插件名称保护，以及 scoreboard 名牌颜色写入的回归测试。

## 3.0.0 - 2026-07-31

### 恢复单竞技场组

- 删除一张竞技场同时加入多个组的运行时行为和 `/bw arenaGroup add|unset` 命令；`set` 现在覆盖唯一分组，删除全局分组时把使用它的地图重置到 `Default`。
- 竞技场配置恢复为单值 `group`。配置架构升级到 21，自动取 2.13.x `groups` 列表的第一项作为原主组，并删除旧列表；管理员的主组选择不会丢失。
- 匹配、选择器、在线人数、加入 NPC、设置界面、PAPI 和大厅套接字统一读取单一分组，不再保留组合组查询的分叉逻辑。

### 死亡流程与性能

- 最终淘汰时只在 `PlayerRespawnEvent` 内完成必要状态切换；计分板、全场 TAB、同场可见性、传送、告示牌和加入 NPC 更新合并到下一 tick，避免在复活事件中形成同步数据包尖峰。
- 淘汰路径不再遍历全服玩家，也不再重建客户端仍持有的全地图生成器和商店悬浮字；主动加入观战时仍执行完整初始化。已经是旁观者的玩家再次死亡也不会重复刷新悬浮字，死亡掉落复用位置对象以减少热路径临时对象。
- 修复竞技场排序比较器对两个重置中地图返回互相冲突结果的问题，改为“状态、人数、名称”的稳定比较器；随机加入 API 在确实没有加入任何地图时正确返回 `false`。

### Paper 1.21.11 性能审查

- 两个玩家移动监听器使用 Paper 的 `hasChangedPosition()` / `hasChangedBlock()` 分层短路：转头不再触发移动逻辑，AFK 只在真实位移时重置，虚空、床悬浮字和基地检测只在跨方块时执行，生成器与商店悬浮字只在跨区块时刷新。
- 开局时建立玩家 UUID 到队伍的只读索引，战斗、TAB、侧边栏、商店和升级热路径优先常数时间查询；淘汰后的历史队伍仍由独立开局快照保留。商店购买、陷阱广播、伤害和侧边栏内部也复用单次查询结果。
- 资源生成器与火球改用 Paper 类型化邻近玩家/物品查询，不再扫描范围内全部实体；`stack-items: true` 会按材质最大堆叠数直接生成最少数量的物品实体，`spawn-limit` 继续按物品总数而不是实体数计算。
- 声音直接以玩家实体为声源，避免热路径创建临时位置；只在初始化时读取不会热更新的交互、特殊商品和生成器分流配置。实现直接使用 Paper 1.21.11 的材质、事件和实体 API，不保留低版本运行分支。

### 火球射程与潜行加速

- 火球不再读取名称具有误导性的 `Fireball#getDirection()` 作为初速度基准，而是直接从玩家瞄准方向构造速度向量；普通默认倍率从 11 提高到 16，对应 1.6 格/tick，在开阔且区块已加载的场景中可快速覆盖约 100–200 格。
- 潜行投掷同时检查 Paper 1.21.11 的当前按键输入和玩家姿态，默认额外倍率从 1.25 提高到 1.5，初速度明确达到 2.4 格/tick；原有轻微后坐力保持不变。
- 火球加速度固定保留原版 0.1，避免通过提高加速度造成客户端预测位置抖动。主配置架构升级到 25，只迁移仍为旧默认值 11/1.25 的速度，自定义参数保持不变。

### 锋利升级价格

- 队伍锋利继续逐级购买到锋利 IV，默认价格调整为 4、6、9、14 钻石；后一级由前一级乘以 1.5 并四舍五入计算，不再按 4、8、16、32 翻倍。
- `upgrades2.yml` 架构升级到 8；迁移器只替换仍等于旧内置默认值的价格，管理员自定义价格保持不变，缺失层级仍按新版顺序补齐。

### API 与维护性

- `IArena` 新增玩家、旁观者、队伍、中央生成器和告示牌的只读快照方法，旧可变集合查询继续保留以兼容现有附属插件。
- 2.13.x 已发布的 `getGroups`、`setGroups`、复数 PAPI 占位符和套接字 `arena_groups` 字段保留为单元素兼容桥，避免附属插件出现 `NoSuchMethodError`；新代码统一使用单数 API。
- 多组规范化工具替换为职责单一的单组策略，竞技场排序和死亡后的视觉同步拆成命名明确的独立逻辑，并增加配置迁移、API 契约、快照只读性和排序回归测试。
- 增加队伍索引生命周期、移动区块边界和生成器实体合并策略回归测试，并更新代码质量审计，明确唯一运行目标为 Paper 1.21.11 + Java 21。
- 本次主动删除多组游戏行为并恢复单值配置格式，按语义化版本规则发布为主版本 3.0.0。

## 2.13.1 - 2026-07-31

### 恢复上游 TAB 样式与光谱排序

- 以 `andrei1058/BedWars1058@669872626ec4160b6896755ba1d79bbed96787fb` 为基准，恢复游戏中、胜负结算和淘汰状态的原始队伍前缀选择；不再用空字符串绕过语言文件中的 `{teamColor}{teamName}` 样式。
- 继续通过 Paper 原生计分板队伍颜色渲染 TAB 玩家名和头顶名称；格式化期间清除会覆盖计分板颜色的自定义 `playerListName`，并在最后一个查看上下文结束后恢复。
- 队伍不再按竞技场配置顺序排列，而是根据 RGB 实时计算色相，按红、黄、绿、深绿、青、蓝、粉、白、灰、深灰排列；同色队伍使用队伍名和运行时标识稳定排序，存活和已淘汰的同队玩家保持在同一组并按完整玩家名字典序排列。
- 玩家最终淘汰时会立即刷新该竞技场所有其他查看者中的 TAB 条目，不再等到周期刷新；按照 1.21.11 客户端“高值在前”的实际规则写入顺序。玩家列表排序仅接管已启用格式化的上下文，退出或关闭格式化后恢复接管前的顺序值。
- 简体中文语言架构升级到 14，把此前由插件迁移为空的内置队伍前缀恢复为上游格式，同时保留非内置的管理员自定义前缀。
- 增加上游样式迁移、RGB 光谱顺序、同色队伍稳定性、旧 `AQUA` 兼容、同队淘汰成员连续性、跨查看者淘汰刷新、玩家列表顺序写入与恢复，以及计分板队伍颜色和成员写入测试。

## 2.13.0 - 2026-07-31

### 队伍显示、选择与开局

- 游戏内 TAB 生命周期不再依赖右侧计分板是否启用；只要当前阶段启用了玩家列表格式或 TAB 页首页尾，就会建立独立计分板上下文。队伍计分板颜色同时作用于 TAB 名称和玩家头顶名称，且保留按查看者渲染的前后缀、隐身可见性和其他插件原有 `playerListName`。
- `/bw team` 现在打开真实的游戏队伍选择 GUI，而不是固定队友邀请列表；支持点击颜色选择、`choose <队伍>` 直接选择和 `clear` 取消。固定队友邀请 GUI 移至兼容入口 `/bw team squad`，原有邀请子命令继续可用。
- 玩家进入等待区时会明确提示 `/bw team`；同一固定小队的选择会一起应用，冲突或无法满足人数范围的选择只作为偏好，不能阻塞合法开局。
- 普通开局的队伍数继续由实时不等式计算：下界为 `max(2, ceil(玩家数 / maxInTeam))`，上界为 `min(地图队伍数, floor(玩家数 / minInTeam))`。资格检查和最终分配共用同一规划器，优先启用最多合法队伍并保持正常大小的小队完整；实现不使用硬编码结果、预生成表或记忆化状态表。

### 菜单、升级、命令与设置

- `/bw gui` 在当前布局容纳不下竞技场时自动扩展至 54 格，旧 7 槽布局在 54 格中恢复为每页 45 个内容槽；翻页目标保存在当前 GUI 持有者中，不再依赖可能丢失的物品自定义数据。
- `upgrades.yml` 架构升级到 7，显式把缺失的锋利 I–IV 配置写回旧文件并保留管理员自定义值；升级层级按数字排序载入，确保购买顺序稳定到锋利 IV。
- `/shout` 正式声明为 Bukkit 插件命令并绑定执行器；即使旧 `allowed-commands` 没有 `shout`，普通玩家也不会在竞技场内被预处理监听器拦截，`bw.shout` 仍默认授予。
- `/bw save` 会在同一次校验中同时列出缺床队伍和缺铁/金生成器队伍，不再因床位错误提前返回而隐藏生成器警告。

### 质量与兼容性

- 新增并扩充分页、命令过滤、配置迁移、升级顺序、TAB 生命周期、队伍数公式、固定小队边界、真实队伍偏好和最终人数范围测试。
- 本次增加向后兼容的真实选队 GUI，因此按语义化版本规则发布为次版本 2.13.0。

## 2.12.1 - 2026-07-30

### 队伍数量实时计算

- 移除分队器中的 `SearchState`、`failedStates` 记忆化状态表，不再保存或查询历史队伍负载组合。
- 合法队伍数下界直接计算为 `max(2, ceil(玩家数 / maxInTeam))`，上界直接计算为 `min(地图队伍数, floor(玩家数 / minInTeam))`；仅当上界不小于下界时才存在合法队伍数量。
- 小队不可拆约束改为依据当前队伍负载、剩余小队人数和未组队人数实时计算；等负载队伍通过相邻数值比较消除等价分支，不建立查找表。
- 增加公式下界回归断言，并在仓库维护规则中明确禁止硬编码配置结果或使用记忆化状态表判断队伍数量合法性。
- 本次不改变配置和公开 API，属于内部计算方式修正，因此发布补丁版本 2.12.1。

## 2.12.0 - 2026-07-30

### 竞技场选择器

- `/bw gui` 默认从 27 格、7 个竞技场槽位扩展为 54 格、每页 45 个竞技场；超过一页时显示上一页、页码和下一页按钮。
- 自定义菜单大小和 `use-slots` 继续生效，翻页时保留竞技场分组筛选；无效或越界槽位会安全忽略，空布局自动回退到可用槽位。
- `config.yml` 架构升级到 24。迁移器只把仍同时使用旧版 27 格和 7 槽位默认值的布局升级为新版，管理员自定义布局保持不变。

### 开局资格与分队

- 倒计时资格与最终分队现在使用同一套精确规划：正常游戏只有在能组成至少两支合法队伍时开始，每支启用队伍均满足 `minInTeam..maxInTeam`。
- 分配器优先启用在队内人数下限范围内能够成立的最多队伍，并把未组队玩家均衡分散，避免随机贪心造成“有合法分法却不开局”或不必要地少开队伍。
- 正常大小的赛前小队和外部队伍保持完整；只有人数已经超过单队上限、客观上无法放进任一队伍的外部大队伍才拆分。一个满员小队不会再被强拆成两队来虚构对手。
- 新增队伍边界回归测试，覆盖单一小队、多个小队、`minInTeam != maxInTeam`、未满容量提前开局、最多队伍分散及贪心失败场景。
- 本次包含兼容的竞技场选择器分页功能，因此按语义化版本规则发布为次版本 2.12.0。

## 2.11.0 - 2026-07-30

### 队伍显示与开局组队 GUI

- 修复玩家设置过自定义 `playerListName` 后，客户端跳过计分板队伍格式、导致游戏内 TAB 名称保持白色的问题；格式化生效期间临时清空自定义 TAB 组件，最后一个查看上下文结束时恢复原值。
- 游戏阶段的计分板队伍颜色现在同时用于 TAB 玩家名称和玩家头顶名称；隐身药水仍会按原逻辑隐藏头顶名称。
- `/bw team` 和 `/bw team list` 现在打开开局组队 GUI：展示当前成员、可邀请玩家和收到的邀请，支持左键接受、右键拒绝及退出当前队伍。
- 玩家进入多人队地图的等待区时会收到 `/bw team` GUI 入口提示；原有 `invite`、`accept`、`decline`、`leave` 子命令保持兼容。

### 战斗、权限与设置校验

- 队伍锋利升级从仅锋利 I 扩展到锋利 I–IV，默认价格为 4、8、16、32 钻石，以对应保护 I–IV；旧的管理员自定义升级值和语言文字会保留。
- `bw.shout` 改为默认授予普通玩家，因此 `/shout` 和 `!消息` 无需额外配权即可使用；服务器仍可显式撤销该节点，`bw.shout.bypass` 仍只绕过冷却。
- `/bw save` 现在逐队检查铁和金生成器；任一必要生成器未设置时会列出队伍和缺失类型并阻止保存，避免启用经济系统不完整的竞技场。
- `upgrades.yml` 架构升级到 6，简体中文语言架构升级到 13，其他内置语言升级到 4；锋利四级键按新版结构迁移并保留自定义值。
- 本次包含兼容的新 GUI 和升级层级，因此按语义化版本规则发布为次版本 2.11.0。

## 2.10.56 - 2026-07-29

### 仓库级维护规范

- 新增根目录 `AGENTS.md`，让后续自动化开发会话在开始工作时直接读取并遵守统一规则。
- 固化语义化版本选择、默认分支直推、自动 Release 核验、版本同步位置、配置迁移和完整构建要求。
- 固化队伍人数不变量，明确 `[minInTeam=3, maxInTeam=4]` 时少于 6 人不能开局，6 人必须分成 `3+3`。
- 本次只增加维护规范，没有改变运行时逻辑，因此使用补丁版本 2.10.56。

## 2.10.55 - 2026-07-29

### 明确队伍下限与版本规则

- 增加 `[minInTeam=3, maxInTeam=4]` 的精确回归场景：少于 6 人不会开始，6 人时只启用两支队伍并分配为 `3+3`，不会产生两支 1 人队。
- 在贡献指南中记录语义化版本规则：兼容修复、性能、测试和文档使用补丁版本；兼容新功能、配置或 API 使用次版本；不兼容变化使用主版本。
- 本次只补充测试与开发规范，没有改变 2.10.54 的运行时匹配逻辑，因此版本递增为补丁版本 2.10.55。

## 2.10.54 - 2026-07-29

### 提前匹配与潜行火球

- `minInTeam` 只在 setup 会话执行 `setType` 或 `setMaxInTeam` 时自动同步为 `maxInTeam`；载入、启用和升级已有竞技场时不再覆盖管理员保存的下限。
- `/bw setMinInTeam <人数>` 恢复为独立设置命令，范围为 `1..maxInTeam`，并加入 setup 命令列表。
- 正常匹配不再等待竞技场满员；只要能够组成至少两支达到 `minInTeam` 且不超过 `maxInTeam` 的队伍就进入倒计时。未被本局选中的空队不参与最终校验。
- 例如 `[minInTeam=1, maxInTeam=2]` 且在线 2 人时，分配器立即选择两支 1 人队，不再等待全部队伍容量填满。
- 普通火球默认速度由 10 提高到 11，使其飞得稍远；迁移器只调整仍等于旧默认值的配置。
- 新增 `fireball.sneak-speed-multiplier: 1.25` 与 `fireball.sneak-recoil: 0.05`。潜行投掷时火球加速，并给发射者约半格的反向水平后坐力；按低摩擦情况将后坐速度硬限制为 0.08，使单次效果不足一格且无需逐 tick 跟踪。
- 主配置架构升级到 23，竞技场配置架构升级到 20；补充提前开局、配置保留、速度迁移和火球物理回归测试。

## 2.10.53 - 2026-07-28

### 按原版 noon 语义锁定正午

- 修正上一版本把 `12000 tick` 误作正午的问题；竞技场和设置世界现在使用原版 `/time set noon` 对应的 `6000 tick`。
- 插件继续直接调用 `World#setTime(6000)`，避免为了使用命令别名而经过命令分派与解析；`doDaylightCycle:false` 仍只在初始化时设置，不恢复周期扫描。
- 时间跳跃监听器同步只放行最终到达 `6000 tick` 的变化；晴天、雷暴和其他自然环境限制保持不变。
- 竞技场配置架构升级到 19，自动更新旧配置中的中文说明，不修改管理员已有的配置值。

## 2.10.52 - 2026-07-28

### 事件驱动的时间与天气锁定

- 删除每 20 tick 遍历全部竞技场与设置会话、重复读写时间和游戏规则的周期任务。
- 改为在世界初始化、世界载入、竞技场初始化或设置会话开始时一次性设置 `doDaylightCycle:false`、`doWeatherCycle:false`、晴天与固定时间。
- 按需求把固定时间从 `6000` 调整为 `12000 tick`；后续只在实际发生时间跳跃时检查目标时间，不进行轮询。
- 下雨与雷暴均由事件监听器阻止；清除天气的变化允许通过，SHARED 模式不影响非 BedWars 世界。
- 竞技场配置架构升级到 18，更新环境规则的中文注释；新增固定时间、雨天和雷暴锁定策略测试。

## 2.10.51 - 2026-07-28

### 修复时间锁定事件递归

- 修复竞技场环境守卫每秒调用 `World#setTime` 时，`TimeSkipEvent` 监听器再次进入环境守卫，造成事件系统递归并持续抛出 `BootstrapMethodError` 的问题。
- 时间跳跃监听器不再于事件回调内修改时间；只放行结果恰好为正午 `6000 tick` 的跳跃，其余原版、命令或其他插件尝试改变的时间仍会被取消。
- 新增正常、跨天及极端数值的时间跳跃回归测试，避免长整数溢出绕过锁定。

## 2.10.50 - 2026-07-28

### 按新版结构整理配置键

- 修复自动迁移只把新键追加到 YAML 末尾的问题；现在会按当前版默认模板的顺序递归重建配置树，新键会出现在对应配置节中。
- `config-version` 固定放在文件顶部；管理员或附属插件的未知键会保留在原配置节，并保持它们之间的原有顺序。
- 配置重建后恢复块注释和行内注释；迁移前的 `.bak` 备份机制保持不变。
- 提升所有内置主配置、竞技场、生成器、商店、升级、声音、等级、经济、告示牌和语言配置架构号，使已有服务器在本次启动时备份并执行一次结构整理。
- 新增配置顺序、嵌套自定义键、注释保留及无默认模板场景的回归测试。

## 2.10.49 - 2026-07-28

### 锁定竞技场环境、满员开局与 NPC

- 竞技场和设置世界始终锁定正午 `6000 tick` 与晴天；`doDaylightCycle`、`doWeatherCycle`、`doMobSpawning`、`randomTickSpeed`、火势蔓延和 Locator Bar 由轻量运行时守卫持续校正。
- `randomTickSpeed` 固定为 `0`，并拦截树叶腐烂、方块生长、蔓延、消退和自然形成事件，防止地图因原版自然机制变化。
- 正常匹配的最少人数自动等于竞技场最大人数；`minInTeam` 自动同步为 `maxInTeam`，所有配置队伍满员后才开始。`/bw start debug` 仍可由 OP 绕过限制测试地图。
- 商店和升级村民关闭 AI、感知和重力；使用单一轻量任务逐 tick 校正位置、身体 yaw 和视线 yaw，未偏移时不发送传送或旋转包。
- 竞技场配置架构升级到 16，旧地图自动补入 `randomTickSpeed:0`、同步人数限制并更新中文注释。

## 2.10.48 - 2026-07-28

### 修复 TAB 队伍数据包导致客户端断线

- 修复 Minecraft 26.2/ViaFabricPlus 客户端可能每隔 60 秒因 `Cannot remove from team 'bw_t_*'` 被断开的问题。
- TAB 行改为使用玩家 UUID 作为稳定身份；队伍、死亡、淘汰、旁观和隐身状态变化只原地更新同一个计分板队伍，不再先删除再重建。
- 计分板队伍名改用当前侧边栏内单调递增且不复用的短 ID，删除 `String.hashCode()` 命名方式，避免哈希碰撞把两个玩家合并到同一队伍。
- 周期刷新先比较前缀、后缀、颜色和名称可见性；内容没有变化时不调用 Bukkit 写接口、不产生冗余队伍数据包，保留动态 TAB 和每 60 秒刷新配置。
- 状态切换按目标玩家集合增量同步，只清理真正离开的行；旁观、队伍颜色、玩家排序和隐身名称隐藏行为保持不变。

## 2.10.47 - 2026-07-27

### 一个竞技场加入多个分组

- 竞技场配置从单个 `group` 升级为有序列表 `groups`；同一地图可以同时通过多个分组进入匹配和竞技场选择器，第一项为主组。
- 竞技场配置架构升级到 15，旧 `group` 会自动迁移为单元素列表并删除；空值、重复值和大小写重复项会自动规范化。
- `/bw arenaGroup set` 现在设置主组并保留其他组；新增 `add`、`unset` 和 `show`，删除全局分组时会同步清理所有竞技场的成员关系。
- `/bw join <分组>`、组合分组、组在线人数、加入 NPC 和选择器均识别全部成员组；同一地图同时命中多个查询组时只计算一次。
- API 新增 `IArena#getGroups`、`isInGroup` 和 `setGroups`，旧 `getGroup` 继续返回主组；代理状态 JSON 额外发送 `arena_groups`，PlaceholderAPI 新增复数分组占位符。

## 2.10.46 - 2026-07-27

### 静默返回代理大厅

- 主大厅“回到主大厅”物品现在立即发送一次 `Connect <lobbyServer>`，不再先发送 `GetServer`、`GetServers` 查询。
- 删除查询等待、重复请求合并、超时任务和代理返回消息监听，点击后不再向玩家显示代理节点、服务器列表、连接进度或失败诊断。
- 配置为空或插件消息发送异常只记录到服务端日志；BUNGEE 模式的竞技场离场重试同样保持静默。

## 2.10.45 - 2026-07-27

### 修复放置方块客户端不同步

- 修复服务器卡顿时方块已经成功放置，但放置者客户端仍显示为空气，直到再次点击对应位置才出现的问题。
- 成功的普通及多方块放置会按玩家收集并去重，在下一 tick 读取服务端真实方块状态后，仅向放置者批量重发；跨世界或已离线玩家的旧批次会直接丢弃。
- 同一玩家每 tick 最多创建一个同步任务，Paper 会按区块段合并 `sendMultiBlockChange` 数据包；不会创建方块状态快照、发送整份背包或为每个方块单独创建任务。

## 2.10.44 - 2026-07-26

### 统一方向数据

- 所有新保存的大厅、加入 NPC、等待区、观战点、队伍出生点、商店和升级 NPC 朝向都会把 yaw 四舍五入到最近的 90 度倍数，并把 pitch 固定为 `0.0`。
- `-88.328 → -90`、`-179.231 → -180`、`83.4312 → 90`；运行时读取也会规范化方向，不能用旧配置绕过。
- 竞技场配置架构升级到 14，主配置架构升级到 21，自动迁移独立 Facing 字段、旧坐标内方向、大厅和加入 NPC 数据。
- API 新增 `ConfigManager.snapYawToCardinal`、`serializeConfigLocation` 和兼容旧扩展的发送者感知命令补全入口。

### 修复设置人数流程

- `/bw setMaxInTeam` 现在同时显示在高级和引导式快速设置列表中。
- 创建或自动识别队伍不再把管理员设置的 `maxInTeam` 强制覆盖为 1/2；`setType` 仍可写入所选类型的标准容量，之后允许手动覆盖。
- `/bw setMinInTeam` 的 Tab 候选按当前 `maxInTeam` 动态生成，不再因快速设置的旧自动覆盖而只能选择 1/2。

### 全地图禁用 Locator Bar

- 除原有插件启动、世界加载和竞技场初始化外，新增 `WorldInitEvent` 阶段强制关闭 Locator Bar，覆盖全部大厅、竞技场及其他已载入地图。
- 保留竞技场应用自定义游戏规则后的二次关闭，配置中的 `locatorBar:true` 不能重新开启导航栏。

## 2.10.43 - 2026-07-26

### 移除中央生成点自动识别

- 删除 `/bw autoDetectGenerators`、全图结构扫描器、快速设置菜单入口、兼容权限节点及对应测试，避免设置线程扫描大范围地图造成卡顿。
- 钻石和绿宝石生成点现在只由 `/bw addGenerator`、`/bw removeGenerator` 手动维护；`/bw save` 继续不会隐式扫描或改写点位。
- 中文竞技场设置、配置和权限文档已同步移除旧功能说明。

### 竞技场固定白天

- 设置世界和已启用竞技场加载时都会把时间校准为 `1000` tick，并关闭昼夜循环；睡眠、命令及其他跳时事件也会在这些世界中取消。
- 固定白天不使用每 tick 定时任务，也不会影响 SHARED 模式中不属于 BedWars 的其他世界。
- 竞技场配置架构升级到 13；旧配置中的 `doDaylightCycle:true` 及其下划线写法会自动规范为 `doDaylightCycle:false`，不能覆盖固定白天规则。
- 新增固定时间常量、昼夜规则迁移和已删除权限节点的回归测试。

## 2.10.42 - 2026-07-23

### 竞技场每队最少人数

- 每张竞技场新增 `minInTeam`，默认 `1`；可在设置会话中使用 `/bw setMinInTeam <人数>` 修改，范围为 `1..maxInTeam`。
- 倒计时只会在当前总人数能够组成至少两支达标队伍时开始；分队器只启用能够全部达到下限的队伍，并优先保留开局邀请队伍。
- 接受或退出开局队伍后会立即重新评估倒计时；不会先倒数到 0 才发现固定队伍导致对手人数不足，也不会在队伍调整为可开局后一直静默等待。
- `TeamAssignEvent` 执行后和正式开局前都会再次校验实际队伍人数，外部插件取消分队也不能产生人数不足的参赛队。
- `/bw start debug` 继续允许管理员绕过最少人数和双队限制进行地图测试。
- 竞技场配置架构升级到 12，自动补入并规范化 `minInTeam`；API 新增带默认实现的 `IArena#getMinInTeam()`。

### 商店固定半身护甲

- 商店永久护甲固定只发放护腿和靴子，队伍色皮革头盔与胸甲在购买升级后继续保留。
- 删除 `config.yml` 的 `shop-settings.sell-full-armor` 开关；主配置架构升级到 20 时自动清理旧节点。
- 新生成的 `shop.yml` 不再写入上半身护甲；商店配置架构升级到 5，自动删除护甲分类中的头盔、胸甲和鞘翅商品。
- 运行时拒绝所有上半身护甲商品，旧文件或自定义配置不能绕过固定半身规则。
- 新增队伍人数策略、配置规范化、商店迁移和护甲槽位测试。

## 2.10.41 - 2026-07-22

### 单人队伍自动公屏聊天

- 游戏开始时实际只有一名玩家的队伍不再启用无意义的队内私聊；普通消息直接发送给当前竞技场全部玩家和旁观者，并使用公屏聊天格式。
- 自动公屏不要求 `/shout` 权限，也不触发喊话冷却；显式 `/shout` 和 `!消息` 的权限及冷却规则保持不变。
- 聊天路由改用开局人数快照，而不是地图的 `maxInTeam` 上限；队友在开局后掉线、重连或被淘汰不会中途改变频道。
- API 新增 `IArena#getTeamSizeAtGameStart(ITeam)`，第三方实现默认回退到队伍当前人数。
- 新增开局人数快照持久性与单人/多人聊天路由测试。

## 2.10.40 - 2026-07-22

### 袖珍弹出塔商店文本修复

- 修复商店商品 ID 为 `tower`，内置语言却把名称与 Lore 写入 `Compact Pop-up Tower` 路径，导致 GUI 显示红色 `Name not set` 的问题。
- 简体中文现在显示“袖珍弹出塔”、正确的价格行，以及自动生成带梯子队伍色防御塔的中文说明。
- 所有语言提供有效的英文后备文本；中文、英文和意大利文使用各自的正确 `tower` 文本。
- 语言配置自动升级：简体中文架构升至 11，其余语言升至 2。迁移会删除无用旧节点，把旧翻译搬到正确路径，并只清理系统占位文本，不覆盖管理员的正确自定义值。
- 新增旧名称/Lore 迁移与自定义文本保护测试。

## 2.10.39 - 2026-07-22

### 火球与火势规则修复

- 修复普通火焰和灵魂火被爆炸保护射线误判为“不可破坏的地图方块”，导致着火表面的玩家放置方块无法被火球炸毁。
- 竞技场现在明确拦截由 `SPREAD` 引起的方块点燃，禁止火势蔓延；火球的 `make-fire` 仍只控制命中处是否生成火焰。
- 适配 Paper 1.21.11 的新游戏规则：竞技场配置架构升级到 11，自动删除已经失效的 `doFireTick`，改写为 `fireSpreadRadiusAroundPlayer:0`，并在设置地图和载入竞技场时强制应用。
- 爆炸射线对空气、火焰和灵魂火采用无注册表的常量判断，避免每条射线产生不必要的注册表查询。
- 新增火焰射线透明性、旧游戏规则迁移和 1.21.11 规则键转换测试。

## 2.10.38 - 2026-07-22

### 竞技场重置安全疏散

- 修复重置倒计时到 0 时，玩家异步返回大厅尚未完成便立刻卸载竞技场世界，随后被硬编码 `You're not supposed to be here.` 踢出的竞态。
- 重置流程改为安全状态机：0 秒先清理竞技场身份并发起返回大厅；竞技场世界保持加载，直到世界中没有玩家且异步传送全部完成后才开始恢复地图。
- 仍滞留世界的玩家每秒重新传送到配置大厅；大厅坐标不可用时选择非竞技场世界出生点。找不到安全目标或传送被取消时保留玩家在线、保留竞技场世界并继续重试。
- `Arena#restart()` 删除硬编码踢人逻辑，并在世界仍有玩家时拒绝卸载；新增 `restartIfEmpty()` 作为内部安全入口。
- BUNGEE 代理切服缓慢或失败时不再以重置消息踢出玩家，而是保留在线并重试代理连接。
- 新增重置卸载门禁测试，确保疏散未开始、世界仍有玩家或传送仍在进行时都不能卸载。

## 2.10.37 - 2026-07-22

### 主大厅消息受众修复

- 修复独立大厅世界尚未加载时，`lobbyWorld` 被缓存为空字符串且世界加载后不再更新的问题；这会让玩家加入/退出提示和竞技场加入提示把全部主大厅玩家错误过滤掉。
- 大厅世界名现在直接从 `lobbyLoc` 配置文本解析，不再要求 Bukkit 已经加载对应世界；延迟世界加载流程也能使用正确名称主动加载大厅。
- 普通玩家进入竞技场时先写入玩家→竞技场映射，再清理大厅状态并广播，消除广播瞬间仍被识别为大厅玩家的竞态；旁观加入路径保持相同顺序。
- API 新增 `ConfigManager#getWorldNameFromConfigLocation(String)`，可在世界未加载时安全读取通用配置坐标的世界名。
- 新增完整与旧版坐标格式的未加载世界名解析回归测试；三类消息仍只面向主大厅及目标准备阶段竞技场。

## 2.10.36 - 2026-07-22

### 返回物品确定性路由

- 主大厅、竞技场等待区和旁观区的返回红床不再共用一个含义模糊的 `RUNCOMMAND_bw leave` 再根据玩家世界猜测目标。
- 新发放的主大厅返回物品带有“代理大厅”持久标记，点击后无条件进入代理连接与中文诊断流程；不再依赖 `lobbyWorld` 识别或管理员是否改过物品命令。
- 等待区和旁观区的返回物品带有“本服 BedWars 大厅”标记，点击后直接调用竞技场离开流程，不再经过命令权限、命令注册或文本匹配。
- 交互处理改为读取事件实际触发手中的物品，修复主手/副手事件读取主手物品造成的误判；旧版未标记物品继续沿用兼容路径。
- 新增返回物品定义识别和持久目标解析测试；点击已标记返回物品后必定执行对应动作或显示明确错误。

## 2.10.35 - 2026-07-21

### 调试日志降噪

- 修复 `/bw start debug` 错误地永久开启全局详细日志；该参数现在只允许本局以单支非空队伍开始，不再修改 `config.yml` 的 `debug` 语义或运行时日志状态。
- 全局调试开关的初始值改为关闭，仍只由 `config.yml` 的 `debug` 显式控制。
- 删除竞技场初始化和每个铁/金/钻石/绿宝石生成器初始化时的逐对象 `DEBUG:` 输出，避免多地图启动时刷屏；真正用于故障诊断的其他调试日志仍受 `debug: true` 控制。
- 中文命令、配置和排错教程补充“单队调试开局”与“详细日志开关”的区别。

## 2.10.34 - 2026-07-21

### 代理主大厅返回修复

- 按 BungeeCord 插件消息协议复核了主大厅切服实现：`Connect` 数据格式原本正确，但该子通道没有成功回执，代理配置错误时会表现为红床点击后完全无反应。
- 主大厅“回到主大厅”红床现在先通过 `GetServer` 和 `GetServers` 验证玩家确实经由代理连接、目标服务器存在且不是当前节点，再发送 `Connect`；重复点击会合并为同一次检查。
- 代理不响应、`lobbyServer` 名称不存在、目标就是当前节点或名称大小写不一致时，玩家和控制台会收到可操作的中文诊断，不再静默失败。
- 补全 Velocity 与 BungeeCord/Waterfall 的中文版配置示例，明确 Velocity 的 `[advanced].bungee-plugin-message-channel = true`、代理 `[servers]` 键名以及禁止绕过代理直连后端的要求。
- 更新所有 `lobbyServer` 中文配置注释；已有配置会在启动保存时获得新说明，不增加或覆盖管理员配置值。
- 新增代理查询数据包、服务器列表解析、去重和名称匹配回归测试。

## 2.10.33 - 2026-07-21

### 队伍侧边栏与淘汰 TAB

- 竞技场进入 `playing` 的瞬间会快照实际有玩家的队伍；开局时就为空的队伍不再出现于游戏中和重置阶段侧边栏，已参战队伍后续被淘汰仍保留展示。
- API 新增 `IArena#getActiveTeamsAtGameStart()`，返回与竞技场队伍顺序一致的开局参战队伍列表；第三方实现默认保持显示所有队伍的兼容行为。
- 游戏中已淘汰玩家的 TAB 格式改为 `&7[&c已淘汰&7] <队伍颜色><玩家名>`，不再把淘汰文字放在名字后面。简体中文语言架构升级到 10，自动迁移旧内置前后缀并保留自定义值。

### 旁观生命值与退出竞态

- 旁观玩家不再获得 TAB 或头顶生命值分数；转为旁观时会立即清除之前作为存活玩家留下的分数，周期刷新也不会重新写入。
- 侧边栏 API 新增 `Sidebar#clearPlayerHealth(Player)`，可以仅清除指定玩家的生命值分数，不影响其他存活玩家。
- 修复玩家退出后延迟侧边栏刷新读取已卸载统计数据的异常：大厅占位符改用可空查询，且离线玩家不再创建或更新侧边栏。
- 新增开局队伍过滤、显式队伍占位符、旁观生命值和简体中文 TAB 迁移回归测试。

## 2.10.32 - 2026-07-21

### 竞技场加入通知范围

- `[BW] 玩家 x 加入了竞技场 y` 现在只在目标竞技场处于 `waiting` 或 `starting` 准备阶段时播报，接收者仅包括该竞技场和 BedWars 大厅中的玩家。
- `playing` 游戏中与 `restarting` 重置阶段完全不发送加入通知；中途加入的旁观者不再打扰游戏玩家或大厅。
- 状态校验收口到统一广播入口，防止普通加入、旁观加入或后续新调用路径绕过；新增四种竞技场状态回归测试。

## 2.10.31 - 2026-07-21

### MULTIARENA 大厅返回与进入提示

- 再次固化 MULTIARENA 主大厅“回到主大厅”红床的路由：只要玩家位于 BedWars 主大厅，默认 `bw leave` 物品就会通过 `BungeeCord` 通道发送 `Connect <lobbyServer>`，不受 MULTIARENA/SHARED/BUNGEE 模式分支限制。
- 等待区与观战区的离开红床仍执行本服 `/bw leave`，不会错误跳过竞技场清理流程。
- 玩家进入 BedWars 大厅时，仅向当时也在大厅的玩家显示 `[BW] 玩家 x 加入了游戏`；竞技场、观战者和设置会话不会收到该提示。
- 修复玩家从大厅进入设置模式或其他世界后，大厅在线状态未清理，导致再次回到大厅时不播报的问题；重复调用仍会去重，每次实际进入只播报一次。
- 新增 MULTIARENA 大厅红床路由和大厅状态退出/重进回归测试。

## 2.10.30 - 2026-07-21

### 代理大厅返回与 Locator Bar

- 主大厅第 9 格“回到主大厅”红床不再调用本服大厅传送；现在无论插件使用 MULTIARENA、SHARED 还是 BUNGEE 模式，都向 `BungeeCord` 插件消息通道发送 `Connect <lobbyServer>`，返回代理配置中的大厅服务器。
- `config.yml` 的 `lobbyServer` 默认仍为 `hub`，新增中文注释和教程说明；空值会拒绝发送并向玩家、控制台给出明确错误，大厅红床不会因此踢出玩家。
- 等待区和观战区的离开红床不受影响，仍通过正常 `/bw leave` 流程返回本服 BedWars 大厅；只有已经位于主大厅的返回红床会切换代理服务器。
- 所有启动时已加载世界和后续加载世界都会主动设置 `minecraft:locator_bar=false`；设置竞技场时再次关闭，竞技场初始化应用自定义游戏规则后也会最终强制关闭，防止配置重新开启。
- `config.yml` 架构升级到 19 并补充 `lobbyServer` 中文说明；竞技场配置架构升级到 10，自动备份并把 `locatorBar`/`locator_bar` 的旧值规范为 `locatorBar:false`；新增代理 `Connect` 数据包、红床路由、规则迁移和注册表键回归测试。

## 2.10.29 - 2026-07-21

### 独立扫描全局生成器

- 新增仅引导式快速设置可用的 `/bw autoDetectGenerators`：管理员在设置竞技场期间主动执行后，插件才会扫描、合并去重并立即保存钻石和绿宝石全局生成点。
- `/bw save` 不再扫描钻石/绿宝石结构，只保留床位校验、设置标记清理和结束设置会话，避免保存时出现不可预期的同步扫描卡顿。
- 自动识别底层现在接受两种严格结构：`3×3` 全部为同种资源块，或只有中心一格为对应资源块、外围不含钻石块和绿宝石块；包含 2～8 格资源块的残缺底座及两种资源块混用仍会拒绝。
- 中层继续要求外围 8 格均为任意楼梯且中心为空气，顶层继续要求 9 格全部为空气；生成位置仍保存为中层中心的 `x.5,y.0,z.5`。
- 设置命令列表新增可点击的扫描入口，权限清单新增兼容节点 `bw.command.autodetectgenerators`，并补充实心/单中心底层、残缺底层、混合资源和权限清单回归测试。

## 2.10.28 - 2026-07-21

### 官方权限行为与喊话权限修复

- 修复游戏内 `!消息`、`shout消息` 等聊天喊话入口没有检查权限的问题；聊天入口和 `/shout` 现在共用同一个权限门，必须拥有官方 `bw.shout`、兼容节点 `bw.command.shout` 或全命令权限。
- 按 BedWars1058 官方权限表恢复无需权限的玩家命令：`/bw`、`join`、`leave`、`gui`、`lang`、`cmds`、`stats`、`teleporter`、`upgradesmenu`、`arenaList`、`/leave` 和 `/party` 均可直接使用；本插件新增的 `team`、`invite` 同样作为普通玩家功能开放。
- `/rejoin` 继续严格要求 `bw.rejoin`，`/shout` 与 `!消息` 继续严格要求 `bw.shout`；`bw.player` 不再隐式授予这两个受限功能。
- 启用竞技场的主权限从源码旧值 `bw.enableRotation` 修正为官方文档中的 `bw.enable`，同时保留旧值和 `bw.command.enablearena` 作为兼容别名。
- `/bw start debug` 恢复为官方定义的严格仅 OP 可用；`bw.*`、`bw.command.*` 和旧 `bw.command.start.debug` 均不能让非 OP 绕过单队开局限制。
- `plugin.yml` 补齐官方权限节点的中文说明，教程按官方节点重写，并新增无需权限命令、喊话绕过、启用竞技场别名、OP 调试开局和权限清单回归测试。

## 2.10.27 - 2026-07-21

### 快速模式自动设置

- 引导式快速设置在保存竞技场时会自动检查并设置全部队伍床位；高级模式不再自动修改床位，缺失时明确要求管理员执行 `/bw setBed <队伍>`。
- 快速模式会自动扫描钻石和绿宝石全局生成器。结构必须严格满足：底层 `3×3` 全部为同种资源块，中层外围 8 格全部为任意楼梯且中心为空气，顶层 `3×3` 全部为空气；保存点为中层中心空气的方块中心。
- 自动发现的点位与已有手动配置合并并按方块坐标去重，不覆盖或删除管理员已有点位；未找到某种结构时给出警告并允许手动补设。
- 扫描范围根据已经手动设置的队伍出生点包围盒和岛屿半径计算，支持不以世界原点为中心的地图，避免遍历无关世界区域。
- 删除从未被调用、会递归传送管理员并仅比较外围材质的旧生成器探测代码，以及对应的无效会话状态。
- 新增严格三层结构、混合楼梯、错误结构拒绝、中心坐标和快速模式隔离回归测试。

## 2.10.26 - 2026-07-21

### 自动建队颜色识别

- `/bw autoCreateTeams` 改为直接按羊毛 `Material` 精确映射队伍颜色，不再通过字符串名称中转；`LIME_WOOL/GREEN_WOOL` 分别创建 `GREEN/DARK_GREEN`，`LIGHT_GRAY_WOOL/GRAY_WOOL` 分别创建 `GRAY/DARK_GRAY`。
- 修复附近羊毛计数始终重复读取中心方块的问题；现在真正检查目标周围 `5×5×5` 范围，并要求至少 5 块同色羊毛，孤立装饰羊毛不会再误建队伍。
- 扫描器不再为每个坐标创建临时 `Location` 对象，并始终扫描设置会话绑定的竞技场世界；确认缓存改用 UUID 且到期自动丢弃，避免长期保留玩家对象。
- API 新增 `TeamColor.fromWool(Material)` 和 `setupName()`；新增全部可选颜色精确往返、深浅绿/灰区分及真实邻近方块计数回归测试。

## 2.10.25 - 2026-07-20

### 主大厅返回物品

- 主大厅第 9 格的“回到主大厅”红床现在直接调用插件内部返回流程，不再把点击动作转成一条需要 LuckPerms 授权的 `/bw leave` 命令；玩家手动执行命令仍正常检查 `bw.command.leave`。
- MULTIARENA/SHARED 模式下，即使玩家已经位于大厅世界，也会传送到 `/bw setLobby` 保存的精确位置和朝向，并重新应用大厅状态与三件固定物品；不再误走只适用于 Bungee 的服务器切换逻辑。
- 从竞技场退出时由竞技场清理流程唯一负责传送，不再先同步传送一次、随后又异步传送一次，减少重复位置数据包。

### 游戏玩家名颜色

- 游戏中有队伍归属的玩家不再在名字前显示队伍名称或队伍字母，只使用记分板队伍颜色渲染玩家名本身。
- 队伍分组和队内字典序排序保持不变；简体中文语言配置架构升级到 9，只迁移插件内置旧前缀，保留其他自定义文本。
- 新增大厅返回物品识别、空队伍前缀和语言配置迁移回归测试。

## 2.10.24 - 2026-07-20

### 开局队伍护甲

- 正式开局时现在会强制覆盖四个护甲槽，为每名玩家装备头盔、胸甲、护腿和靴子组成的完整皮革套装，并按所属队伍颜色染色。
- 开局装备不再依赖“槽位必须返回 `null`”的脆弱判断；空物品槽同样会被正确识别。
- 四件护甲一次写入，并在全部开局物品处理完成后只执行一次客户端背包同步；不会逐件触发完整背包同步。
- 复活流程保持原有语义：只补齐空缺的皮革甲，不覆盖玩家已经购买的永久护甲。
- 新增完整皮革四件套顺序及空槽识别回归测试。

## 2.10.23 - 2026-07-20

### 竞技场设置建造

- 竞技场设置会话中的管理员现在可以在正在设置的地图内正常放置方块，不再被“非游戏阶段禁止建造”或“未加入竞技场”保护误拦截。
- 豁免同时覆盖进入设置地图和右键放置所需的保护链路，避免方块放置事件触发前就被取消。
- 豁免严格绑定设置会话的目标世界；大厅、其他竞技场以及正常等待/游戏阶段的建造保护保持不变。
- 新增设置世界名称匹配回归测试。

## 2.10.22 - 2026-07-20

### 主大厅物品与 OP

- 恢复主大厅第 9 格的“回到主大厅”红床，命令为 `bw leave`；新配置直接生成，旧 `config.yml` 升级到架构 18 时自动备份并补回。
- 迁移只填写 `lobby-items.leave` 中缺失的命令、材质、数据值、附魔和槽位，不覆盖管理员已有的自定义设置。
- 主大厅背包、命令物品、丢弃、换手、使用和合成保护现在只限制非 OP；OP 可以正常移动、装备和丢弃物品。
- OP 重新进入主大厅时仍会应用冒险模式并重新发放大厅固定物品，随后可以自行修改。
- 新增主大厅红床安全迁移和 OP 背包放行回归测试。

## 2.10.21 - 2026-07-20

### 单队调试开局

- `/bw start debug` 现在可以在只有一支非空队伍时强制开始游戏；没有任何玩家或队伍时仍拒绝开局。
- 绕过标记只属于当前竞技场的当前开局任务，不会影响其他竞技场；普通 `/bw start` 和自动匹配仍要求至少两支非空队伍。
- 新增独立权限 `bw.command.start.debug`，需要同时具备普通强制开局权限；OP、`bw.*` 和 `bw.command.*` 自动具备。
- 调试倒计时开始后即使人数降到一人，也不会被普通最低人数检查取消；若所有玩家离开，最终分队检查仍会安全终止。
- 开局任务 API 新增只读的 `isSingleTeamDebugStart()` 状态，内置分队器与最终开局校验共同读取，第三方任务默认返回 `false`。
- 新增单队调试策略、分队器门控、命令参数和权限清单回归测试。

## 2.10.20 - 2026-07-20

### 火球平衡

- 略微降低火球默认威力：爆炸范围 `3.5 → 3.25`、水平击退 `1.25 → 1.15`、垂直击退 `0.8 → 0.75`、敌方伤害 `4.0 → 3.5`。
- 飞行速度、冷却、自身伤害、队友伤害和是否点燃保持不变，火球仍可用于破防和位移。
- `config.yml` 架构升级到 17；升级前自动备份，并且只迁移仍使用旧内置默认值的参数，管理员自定义数值不会被覆盖。
- 新增默认参数迁移回归测试。

## 2.10.19 - 2026-07-20

### 可配置护甲套装

- `config.yml` 新增全服开关 `shop-settings.sell-full-armor`：`true` 售卖头盔、胸甲、护腿、靴子四件套，`false` 保持只售卖护腿和靴子；默认开启全身模式，修改后需完整重启。
- 永久护甲缓存、死亡恢复、重连恢复和队伍保护附魔会自动跟随该模式；来回切换不会改写价格或自定义商品。
- `shop.yml` 配置架构升级到 4。升级前自动备份旧文件；迁移器只补齐仍使用内置护腿和靴子材质的商品，不复活已删除商品，也不覆盖管理员自定义材质和已有上装。
- `config.yml` 配置架构升级到 16，旧配置会自动备份并补入护甲模式开关和中文注释。

### 游戏 TAB

- 游戏玩家名统一使用所属队伍颜色；淘汰后旁观的玩家继续使用原队伍颜色，无队伍旁观者保持白色。
- 玩家列表按竞技场配置中的队伍顺序分组，每个队伍内按玩家名字典序排列；淘汰玩家留在原队伍分组，无队伍旁观者排在末尾。
- 排序改用 Paper 原生玩家列表顺序，并把同一 tick 内的多次侧边栏更新合并为一次；顺序未变化时不发送更新数据包。

### 验证

- 新增护甲模式、全套护甲安全迁移、保留自定义商品以及队伍分组/字典序排序回归测试。

## 2.10.18 - 2026-07-19

### 大厅 TAB 与加入通知

- 大厅 TAB 默认宽度由 104 个空格增加到 128 个空格；竞技场内继续保持原仓库的 104 空格宽度。
- 简体中文语言配置架构升级到 8：只把未修改的旧内置大厅页首写回为新宽度；自定义文字不会被覆盖，运行时会把不足 128 的宽度补齐，更宽设置保持不变。
- 玩家或旁观者首次加入竞技场时，通知现在同时发送给该竞技场内的玩家、旁观者和主大厅玩家，不会泄漏到其他竞技场或地图设置会话。

### NPC 放置保护

- 商店与队伍升级 NPC 的保护范围改为按方块坐标对称判断，最少保护 NPC 身体周围一格；修复 x.5/z.5 坐标导致负方向和对角位置漏判的问题。
- 多方块放置只要有任一方块进入 NPC 保护范围，就取消整个事件；取消的方块不会进入玩家放置方块索引。

### 验证

- Maven 反应堆共 99 项测试通过，其中插件模块 79 项、API 模块 20 项。

## 2.10.17 - 2026-07-19

### 大厅保护

- 大厅丢弃保护不再只依赖缓存的大厅世界名称。大厅坐标缺失、进入流程临时使用默认世界或竞技场映射尚未建立时，非竞技场世界中的大厅玩家仍会被保护；取消 Q/Ctrl+Q 后会在下一 tick 同步背包，避免客户端短暂显示物品已被扔出。
- 主大厅不再发放无意义的“回到主大厅”红床。`config.yml` 架构升级到 15，启动时会自动删除旧的 `lobby-items.leave`，但保留等待大厅和旁观状态下真正有作用的离开物品。

### 命令权限

- 每个 `/bw` 一级子命令以及 `/shout`、`/leave`、`/rejoin`、`/party` 都增加独立的 `bw.command.<小写命令名>` 权限点；旧管理权限继续兼容。
- 新增 `bw.player` 常用玩家权限集合和 `bw.command.*` 全命令集合。`bw.player` 由代码直接识别，不依赖权限插件是否处理 Bukkit 子权限继承。
- 无权限的 `/bw` 子命令不会出现在 TAB 补全中，竞技场设置会话也不再绕过命令可见性权限检查。

### 测试

- 新增大厅回退世界、旧大厅物品迁移、独立命令权限、旧权限兼容和 `bw.player` 权限清单回归测试。

## 2.10.16 - 2026-07-19

### BUG 修复

- `/bw upgradesmenu` 现在只会在游戏进行中、玩家已分配队伍且位于本队升级 NPC 四格内时打开；等待阶段或队伍为空时不再抛出空指针异常，升级菜单缺失时也会安全结束。
- 基地进入、死亡、治疗池效果和陷阱检查统一使用跨世界安全的距离判断。即使竞技场映射尚未清理而玩家已经位于大厅，也不会再用大厅坐标与竞技场床坐标直接计算距离。
- `shop.yml` 加入 `config-version` 后，商店加载器现在只解析真正的配置节，并显式排除 `shop-settings`、`quick-buy-defaults` 与 `shop-specials`；不再把配置版本号误报成缺少材质的商品分类。
- 旁观传送器在没有存活且已分配队伍的目标时不再创建 0 格背包；目标列表为空会安全关闭，分页始终限制为 9–54 格，刷新期间失效的玩家和空队伍也会被过滤。

### 测试

- 新增升级菜单空队伍/跨世界、基地跨世界死亡位置、商店配置根节点过滤和旁观传送器空目标分页回归测试。

## 2.10.15 - 2026-07-19

### 队伍与提示

- 队伍名称现在始终使用竞技场 `Team.<名称>` 的原始名称，不再读取语言文件中的 `team-name-*` 翻译，也不再在聊天中强制转为大写。
- 游戏中 TAB 提示由“你正在为 x 队而战”改为“你属于 x 队”。旧版内置中文会自动迁移，管理员自定义文本保持不变。
- 玩家进入或离开大厅时，仅向当前大厅玩家广播 `[BW] 玩家 x 加入了游戏/离开了游戏`；进入竞技场时向该竞技场广播 `[BW] 玩家 x 加入了竞技场 y`。

### 竞技场邀请

- 新增 `/bw invite <玩家>`，允许等待中竞技场的玩家邀请大厅玩家进入同一竞技场匹配。
- 邀请 30 秒内有效，带可点击的接受和拒绝按钮；接受时重新校验双方位置、邀请者所属竞技场、游戏状态及容量，不能使用旧消息绕过开局或满员限制。
- 新增 `/bw invite list`、`/bw invite accept <邀请者>` 和 `/bw invite decline <邀请者>`。

### TAB、粒子与配置迁移

- 新增 `scoreboard-settings.tab-header-footer.lobby-header`，仅覆盖大厅 TAB 顶部文字；空列表继续使用原仓库 104 空格宽度的语言模板，大厅页尾和其他状态不受影响。
- 携带 TNT 的头顶红色粒子不再发送给携带者本人，只发送给 32 格内的其他玩家，向上搭建时不再遮挡准星；同时修复一个隐身携带者导致后续玩家粒子全部中断的问题。
- 主配置架构升级到 14，简体中文语言架构升级到 6；升级会备份并补充新配置和中文注释。
- 新增邀请有效期、大厅受众、TAB 大厅页首覆盖及中文消息迁移回归测试。

## 2.10.14 - 2026-07-19

### TAB 宽度

- 严格使用 `andrei1058/BedWars1058` 上游源码的 104 空格 TAB 页首宽度，不再根据中文字符、最长文本或运行时内容计算宽度。
- 大厅、等待、开局、游戏、淘汰、旁观及三种结算页首共 12 个模板统一引用同一个宽度常量，避免某个状态以后单独偏离上游。
- 增加宽度回归测试，验证常量长度恰好为 104 且只包含普通空格。

## 2.10.13 - 2026-07-19

### TAB 样式恢复

- 以 `andrei1058/BedWars1058` 上游 `master` 提交 `66987262` 为基准，恢复原仓库按大厅、等待、开局、游戏、旁观及胜负状态选择 TAB 页首/页尾模板的逻辑。
- 简体中文 TAB 恢复原仓库的行数、宽度占位、动画、地图模式、本局统计、队伍及胜负信息布局；所有可见英文均翻译为中文。
- 删除 2.10.12 临时加入的全局 `tab-header-footer.header/footer` 配置，旧配置会自动清理并恢复原版等待/开局玩家行开关。

### 保留的兼容修复

- 保留 Paper 1.21.11 下 TAB 生命周期独立于右侧计分板、关闭时清理页首页尾、持久占位符及游戏玩家名使用队伍颜色的修复。
- 保留 2.10.12 的聊天栏重置倒计时和低数据包广播策略。

### 配置迁移

- 主配置架构升级到 13，简体中文语言配置升级到 5；仅替换插件自带的紧凑 TAB 模板，管理员自定义模板及自定义宽度不会被覆盖。

## 2.10.12 - 2026-07-19

### TAB 显示

- 恢复经典的全局 TAB 页首和页尾，删除自动插入的横线撑宽逻辑；大厅、等待区和游戏中共用稳定布局。
- 游戏进行时，玩家列表及头顶名字统一渲染为各自队伍颜色，不再把队友强制标成单独的绿色。
- 新增可配置的 `scoreboard-settings.tab-header-footer.header/footer` 中文注释，旧配置会自动补齐，旧 `teammate-color` 项会自动删除。

### 竞技场重置

- 默认重置倒计时由 45 秒迁移为 60 秒。
- 重置提示改到聊天栏，仅在 `60、30、15、10、5、4、3、2、1、0` 秒广播，不再每秒刷新操作栏。
- 玩家会保留在竞技场直到收到 `0` 秒提示；生成器与全息图在结束前分阶段停用，随后再移出玩家并重置世界。

### 测试

- 增加重置广播时间点、经典 TAB 配置迁移和倒计时默认值迁移测试。

## 2.10.11 - 2026-07-19

### BUG 修复

- 将队伍颜色 `AQUA` 统一更正为 `CYAN`；自动创建队伍现在识别 `CYAN_WOOL`，不再把淡蓝色羊毛误认为青色。
- 青色队伍从商店购买羊毛时现在获得 `CYAN_WOOL`，队伍床、玻璃、玻璃板、陶瓦、染料和皮革装备颜色同步使用青色映射。

### 配置与 API 兼容

- 竞技场配置架构升级到版本 9，旧 `Team.<队伍>.Color: AQUA` 会自动备份并迁移为 `CYAN`。
- 新增 `TeamColor.CYAN`、`TeamColor.fromName` 和不包含旧别名的 `selectableValues`；保留弃用的 `TeamColor.AQUA` 二进制兼容入口，但其方块和物品映射也已改为青色。
- Minecraft 没有独立的青色聊天代码，因此文字仍使用最接近的 `ChatColor.AQUA`，不会影响方块或商店物品颜色。

本文档记录 SimpMC-BedWars 2.x 系列面向服务器管理员和玩家的变化。

## 版本规则

从 `2.10.0` 之后，版本格式固定为 `2.10.y`：

- `2.10`：固定版本前缀，不再因新功能或 BUG 修复修改。
- `y`：每次发布新 JAR 时递增 1，不区分新功能、BUG 修复或性能优化。
- 仅修改文档、测试或开发流程且不发布新 JAR 时，不修改插件版本号。
- 已经发布的历史版本不回溯改号，避免同一版本号对应多个不同 JAR。

示例：

- `2.10.0` 的下一次发布为 `2.10.1`。
- `2.10.1` 之后无论加入功能还是修复 BUG，下一次发布均为 `2.10.2`。

## 尚未发布

暂无。

## 2.10.10 - 2026-07-19

### TAB 显示

- 为所有 TAB 头尾模板提供适中的 30 字符最小可见宽度；只在模板确实过窄时补充一条分隔线，不恢复旧版超长空格占位。
- 大厅 TAB 生命周期与右侧大厅计分板解耦：即使关闭大厅计分板或侧边栏内容为空，TAB 头尾和玩家格式仍会正常创建、刷新及清理。
- 大厅玩家列表格式默认开启；配置架构升级到 11，旧版默认关闭值会自动迁移为开启。
- TAB 玩家列表中的额外生命值数字默认关闭，只保留 Minecraft 原版网络延迟图标；生命值仍可显示在玩家头顶下方，避免被误认为两个不同的 ping。

### 大厅保护

- 大厅丢弃保护现在同时覆盖 MULTIARENA 和 SHARED，且不再允许建造会话通过 Q 或 Ctrl+Q 丢弃物品。
- 增加大厅世界判定、TAB 最小宽度以及 TAB 默认配置迁移回归测试。

### 自动发布

- 默认分支每次提交在 Maven 验证成功后自动创建 GitHub Release，并上传同一次工作流构建的插件 JAR 与 SHA-256 校验文件。
- Release 使用 `v版本号-提交前八位` 标签；同版本的文档或流程提交不会覆盖旧产物，工作流重跑也不会重复发布。
- 发布写权限与 Pull Request 构建隔离，只有默认分支的发布任务拥有 `contents: write`；快速连续提交排队执行，不再取消尚未发布的旧提交。
- 发布任务显式传递仓库标识，无需检出源码也能通过 GitHub CLI 创建 Release。
- 删除两个长期停用且已被新流程取代的部署工作流。

## 2.10.9 - 2026-07-19

### 团队箱子快捷存入

- 游戏中按住 Shift 左键点击本队岛屿的普通箱子或陷阱箱，会把主手物品自动存入；箱子空间不足时只移动可容纳的数量。
- 永久商店物品和出生物品继续受保护，不会被快捷操作误存；双箱会使用完整容量。
- 团队箱子归属改为选择保护半径内最近的队伍出生点，重叠半径下不再依赖队伍遍历顺序。

### 商店批量购买

- 在快速购买页或分类商店内按住 Shift 右键点击商品，会使用背包中对应物品货币一次购买可负担的最大整数数量；Shift 左键原有的快速购买管理行为保持不变。
- 永久商品、多级商品、命令商品、免费商品和 Vault 商品仍按单次购买处理，避免重复执行命令、越级或意外清空经济账户。
- 批量购买只触发一次结算、背包同步、提示音和购买消息，并把产物合并为合法堆叠，避免逐件购买产生大量数据包。
- `ShopBuyEvent#getPurchaseCount()` 现在会向附属插件公开本次操作实际购买的份数；旧构造器继续按单份购买兼容。
- 增加团队箱子部分容量、批量购买数量和产物堆叠回归测试。

## 2.10.8 - 2026-07-19

### 关机日志

- 正常卸载竞技场由 `WARN` 改为黄色 `INFO`，不再把预期的关机流程误报为警告。
- 卸载时仍滞留在竞技场世界的玩家提示改为中文。
- 使用 Paper 的结构化组件日志输出颜色；不支持 ANSI 颜色的控制台会安全显示为普通 INFO 文本。

## 2.10.7 - 2026-07-19

### 资源与经济节奏

- 默认铁配置间隔由 2 秒缩短为 1 秒，数量保持每次 2 个；结合倒计时修复，实际稳定产出约为旧版的 3 倍。
- 默认金配置间隔由 6 秒缩短为 4 秒，数量保持每次 2 个；结合倒计时修复，实际稳定产出比旧版提高约 75%。
- 修复生成器实际间隔比 `delay` 多 1 秒的问题；现在配置的秒数与真实刷新周期一致，0 或负数会安全限制为 1 秒。
- `generators.yml` 架构升级到 4：只迁移仍使用旧内置默认值的 `Default` 节，自定义默认值和竞技场分组值保持不变。
- 同步增强四级团队熔炉的默认生成参数，防止新版基础生成器快于低级熔炉；`upgrades2.yml` 自定义动作不会被覆盖。
- 增加生成器倒计时、默认值和熔炉动作迁移回归测试。

### 配置教程

- 补充 `generators.yml` 的完整路径、默认铁金参数、分组优先级和 `delay`/`amount` 调整方式。
- 修正 `shop.yml` 示例结构，并注明普通商品与团队升级的准确价格路径。

## 2.10.6 - 2026-07-19

### Vault 兼容性

- 不再通过固定插件名判断 Vault，改为直接查询 Bukkit 服务注册表，并监听经济、聊天服务的延迟注册和注销。
- 控制台会区分“没有 Vault API”和“有 Vault 但没有经济服务提供者”，并显示实际接入的服务与提供插件。
- 修复商店错误调用 Vault 银行账户扣款的问题，现在从购买玩家的个人账户扣款。
- 金币监听器始终注册，但在没有经济服务时立即返回；对局中的每分钟奖励也会随服务可用状态动态启停。
- 新增 Vault 服务识别和玩家账户扣款回归测试。

### 仓库中文化

- 完整重写中文首页、贡献指南、致谢、Issue 模板、Pull Request 模板和 GitHub Actions 显示文本。
- 升级 GitHub Actions 到支持当前 Node.js 运行时的主版本，清除 Node.js 20 弃用警告。
- 为安装、配置和排错教程补充 Vault 桥接层与经济服务提供者的区别。
- 删除重复且长期未维护的旧英文 `CHANGELOG`，统一使用中文 `CHANGELOG.md`。
- 插件元数据说明改为中文。

## 2.10.5 - 2026-07-19

### 开局与重置流程

- 删除竞技场 `minPlayers` 配置；旧文件升级到架构 8 时自动备份并清除此节点。
- 倒计时固定在至少两名玩家时开始，分队器和最终开局阶段继续双重保证至少两支非空队伍；附属分队器返回无效结果时会回滚成员缓存和床全息图。
- 游戏结束后每秒通过操作栏提示竞技场剩余重置时间；重置阶段 TAB 的 `{time}` 也改为读取真实重置任务。

### 大厅与火球

- 大厅保护新增丢弃事件拦截，玩家不能把固定大厅物品扔出背包；管理员建造会话仍保留原有例外。
- 火球默认范围由 3 增强到 3.5，水平击退由 1.0 增强到 1.25，垂直击退由 0.65 增强到 0.8，敌方伤害由 2.0 增强到 4.0；旧默认值自动升级，自定义值保持不变。
- 修复多人处于同一次爆炸范围时击退向量被重复修改的问题，限制火球只影响射手所在竞技场的参赛玩家，并避免把队友击退错误记录为敌方最后一击。

### TAB 与语言

- 删除简体中文 TAB 模板中用于强制宽度的大段空格，压缩头部、底部和队伍前缀。
- 完整汉化等待、开局、游戏、淘汰、胜负和旁观 TAB 文本，并修复开局旁观者前后缀错误共用玩家配置路径的问题。
- 简体中文语言架构升级到 4：仅替换旧内置英文模板；自定义中文模板只清理超过 32 个字符的纯空格占位行。

### 测试

- 增加开局策略、火球击退、简体中文 TAB 迁移和配置路径回归测试。

## 2.10.4 - 2026-07-19

### 代码结构与性能

- 将玩家放置方块的记录、清理、重力方块迁移和活塞保护从大型 `BreakPlace` 监听器拆分到独立的 `PlacedBlockListener`，权限判断与方块生命周期不再混杂。
- 建图会话改为按 UUID 存储的集合，查询由线性扫描变为平均 O(1)，玩家离线时立即清理，避免长期持有 `Player` 对象。
- 出生点、商店、升级和生成器保护统一复用同世界平方距离判断；每次事件只读取一次保护半径，并删除会吞掉配置错误的宽泛异常捕获。
- 删除关闭背包时遍历全部物品但不产生任何结果的无效监听，减少无意义的主线程工作。

### 修复与防绕过

- 补全受保护商店物品的拖拽、数字键和副手交换检查，阻止物品进入外部容器；取消事件时不再强制关闭界面，减少额外界面同步数据包。
- 保护半径现在包含配置指定的边界值。

### 测试与维护

- 增加保护半径边界与背包拖拽槽位回归测试。
- 仓库开发工作统一回到默认分支 `paper-1.21.11`，清理已合并或内容重复的临时远程分支。

## 2.10.3 - 2026-07-18

### 方块所有权与性能

- 玩家放置方块改用竞技场独立的唯一坐标索引，添加、删除和判断由线性扫描优化为平均 O(1)。
- 只记录最终成功的普通及多方块放置；自动点燃 TNT 和取消的弹射塔物品不再留下幽灵坐标。
- 爆炸、燃烧、褪色与重力方块移动会同步清理或迁移标记；禁止破坏地图时禁止活塞移动原生方块。
- `IArena#getPlaced()` 改为脱离内部状态的兼容快照并弃用，新增不可变的 `getPlacedBlocksSnapshot()`。

### 安全修复

- 阻止未加入对局的玩家通过其他传送插件进入竞技场，并为放置、破坏、桶、PVP 和拾取增加世界归属复核。
- ZIP 恢复会在写入前检查所有条目，拒绝 `../`、绝对路径等目录逃逸。
- 设置、克隆、删除和分组竞技场时统一拒绝危险世界名，内部恢复 API 同样执行校验。
- 大厅套接字限制单条输入为 64 KiB，校验必要字段及 UUID，畸形输入不再终止读取任务。
- 修正 VipFeatures 方块事件直接修改旧列表及床保护判断不准确的问题。
- 主配置架构升级到 9，并为大厅套接字、数据库密码和 TLS 设置补充安全注释。

### 文档与测试

- 新增中文版安全审计与部署加固文档，明确仍未解决的套接字认证、数据库部署和依赖扫描边界。
- 新增方块唯一性/快照、ZIP 路径穿越、世界名路径穿越和套接字输入上限回归测试。

## 2.10.2 - 2026-07-18

### API

- 新增 `PreGameSquad` 公共服务，附属插件可发送、接受、拒绝邀请，并读取不可变的开局队伍成员和可邀请玩家快照。
- `ArenaUtil` 新增返回 `Optional` 的安全竞技场查询、不可变竞技场列表快照和完整大厅状态应用方法。
- `Configs` 新增等级、Vault 奖励和声音配置访问入口。
- 保留原有可空查询和可变列表方法，避免破坏现有附属插件。

### 配置与迁移

- 为 `config.yml`、所有竞技场、生成器、商店、升级、声音、等级、经济奖励、告示牌和语言文件补充中文头部及分项注释。
- 相关配置架构版本自动递增；旧配置升级前会创建备份并在保留原值的同时写入注释。
- 竞技场每次保存时重新补充动态队伍注释，新创建的队伍同样有字段说明。

### 文档

- 新增完整 GitHub 中文教程，覆盖安装、更新、服务器模式、竞技场设置、全部配置、命令权限、API 开发、代码质量审计和故障排查。
- README 增加中文教程入口。

### 代码质量

- 新 API 优先使用不可变快照和明确的可空结果，减少外部插件误改内部注册表和空指针风险。
- 中文配置说明集中管理，减少同一路径在多个配置类中重复维护。

## 2.10.1 - 2026-07-18

### 行为调整

- 游戏通过开局校验后，直接删除玩家当前背包、装备栏及末影箱中的全部游戏内物品，再发放本局默认装备和物品。
- 聊天名称与消息之间的白色分隔符由 `>>` 改为 `>`，分隔符之后的默认消息颜色由白色改为灰色 `&7`。
- 运行时会把旧语言配置中的冒号、`>>` 或旧颜色格式统一规范为新的 `&f> &7{message}`，无需管理员手动修改语言文件。

### 版本管理

- 从本版本开始，仅递增版本号最后一位。

## 2.10.0 - 2026-07-18

### 新功能

- 大厅玩家不能移动或拖拽物品、给自己装备物品、交换主副手、使用或消耗原版物品以及进行合成；管理员显式进入大厅建造模式后不受此限制。
- 每次游戏通过双队校验并正式开局时重新清空游戏内末影箱，等待阶段放入的物品不会带入本局；MULTIARENA/SHARED 模式仍会在离场时恢复玩家进入竞技场前的末影箱。

### BUG 修复

- 语言文件扫描现在只接受严格的 `messages_<语言>.yml` 文件，不再把 `messages_zh_cn.yml.v1.bak` 等自动迁移备份加载成 `zh_cn.v1.bak`，从而消除重复的缺失语言键报错。

### 性能与实现

- 大厅保护直接复用 Bukkit 事件，不增加轮询任务。
- 末影箱只在成功开局时清空一次，不在倒计时或队伍校验失败时重复操作。

## 2.9.0 - 2026-07-18

### 新功能

- 大厅入口状态统一：玩家每次进入大厅都会切换为冒险模式；OP 进入后仍可通过指令主动修改，下一次进入大厅时再次恢复冒险模式。
- 竞技场等待点、观战点以及队伍出生/复活点会单独保存玩家的 `yaw,pitch`，传送后保留设置时的朝向；位置坐标仍使用方块中心格式。
- 竞技场配置自动升级到版本 6，并迁移旧位置中的朝向字段；缺失的旧朝向使用 `0.0,0.0`。
- 开局分队强制产生至少两支非空队伍；仅当完整保留邀请组会造成单队游戏时，才拆分一名玩家到对手队伍。

### BUG 修复

- 修复大厅状态流程把玩家设置为生存模式的问题。
- 修复 `minPlayers` 小于 2 或满员邀请组恰好占满一队时，游戏可能以单支队伍开始的问题。

### 性能与实现

- 大厅模式只在实际进入大厅时设置，不增加循环检查；语言切换等物品刷新不会反复修改玩家模式。
- 分队结果先完整校验，再创建队伍成员和床全息图，避免失败开局留下部分运行时状态。

## 2.8.0 - 2026-07-17

### 新功能

- 区分普通死亡和虚空死亡：普通死亡在原地进入旁观，虚空死亡回到队伍安全出生点。
- 玩家主动进入虚空且近期没有有效攻击者时，显示“自走虚空”。
- 玩家聊天统一使用白色 `>>` 分隔玩家名称和消息。
- 自动迁移简体中文的旧聊天格式和虚空死亡提示。

### BUG 修复

- 修复竞技场销毁后，侧边栏和奖励定时任务继续读取已清空数据而持续产生 NPE 的问题。
- 修复竞技场销毁时部分定时任务、记分板和生成器没有完整注销的问题。
- 删除死亡旁观的重复模式切换和冻结回退逻辑。

### 性能

- 删除死亡时面向全场玩家的隐藏操作。
- 合并复活时重复的玩家显示和出生同步，只保留一次必要同步。
- 删除不再使用的旧复活数据包补丁代码。

## 2.7.0 - 2026-07-17

### 新功能

- 禁止授予原版进度和成就。
- 商店及升级村民单独保存朝向，旧竞技场自动计算并迁移朝向。
- 大厅自动补齐历史战绩、竞技场选择和离开三件物品。

### BUG 修复

- 修复死亡旁观模式的调用顺序。
- 修复从竞技场或设置模式返回大厅后游戏模式和物品不正确的问题。
- 加强大厅 PVP 保护。

## 2.6.0 - 2026-07-17

### 新功能

- 增加开局前组队邀请、接受、离开及队伍查看命令。
- 未组队玩家按单人队处理；队伍不足时自动随机分配剩余玩家。
- 增加独立的队伍分配规划器及测试。

## 2.5.0 - 2026-07-17

### BUG 修复

- 改进死亡旁观和复活过程。
- 修复从竞技场设置模式返回大厅时的传送、物品和模式恢复。
- 修复商店、升级 NPC 及床显示生命值的问题。
- 加强原版生物生成限制。

## 2.4.0 - 2026-07-17

### 新功能

- 增加 `/bw listTeams`，列出当前竞技场的所有队伍。
- 增加安全出生点搜索，支持一格高空间并使用趴下姿态。
- 在玩家列表中使用特殊颜色标记队友。
- 合并 Spawn 和 Respawn 配置，移除重复位置声明。

## 2.3.0 - 2026-07-17

### 新功能

- 将出生点和复活点的处理分离，为后续安全出生逻辑提供基础。
- 增加断线 30 秒后自动视为离开的处理。

### BUG 修复

- 修复部分玩家不能使用 `/shout` 的问题。
- 修复旧位置格式在新 Paper 版本上的解析问题。

## 2.2.0 - 2026-07-16

### 新功能

- 为主要配置文件增加中文注释。
- 将面向玩家和管理员的硬编码提示汉化。
- 增加配置项注释写入能力。

## 2.1.1 - 2026-07-16

### BUG 修复

- 修复旧大厅坐标字段数量不一致导致的 `ArrayIndexOutOfBoundsException`。
- 自动迁移旧大厅坐标格式。
- 清理重复和失效的旧配置内容。

## 2.1.0 - 2026-07-16

### 新功能

- 建立自动配置版本迁移、备份及废弃配置清理机制。
- 新竞技场自动禁止昼夜变化和生物自然生成。
- 自动寻找各队伍的床。
- 竞技场标点统一保存为方块中心坐标。

### 性能

- 优化数据库、治疗池、竞技场载入和配置访问路径。
- 增加配置迁移及位置解析测试。
