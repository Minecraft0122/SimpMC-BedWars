# SimpMC-BedWars 仓库维护规则

本文件适用于整个仓库。自动化开发工具和维护者开始修改前必须先阅读并遵守；若用户在当前任务中给出更具体的新要求，以用户的最新要求为准，并同步更新本文件中已经过时的规则。

## 分支与发布

- 直接提交并推送到 GitHub 默认分支，当前为 `paper-1.21.11`；不要创建或继续使用 `codex/*` 临时分支。
- 默认分支的每个提交都会由 `.github/workflows/compile_snapshot.yml` 自动构建、测试并创建 GitHub Release。
- 推送后必须等待工作流结束，并核对 Release 中实际下载的 JAR 文件名、`plugin.yml` 版本和 SHA-256；不能只报告“已经推送”。
- 提交前先获取远端并确认本地与默认分支没有意外分叉。不要覆盖或删除用户已有的无关改动。

## 版本号

版本号由变更的实际兼容性决定，使用 `MAJOR.MINOR.PATCH`，不再固定只修改 `2.10.x` 的最后一位：

- `PATCH`：向后兼容的错误修复、性能优化、重构、测试或文档完善。
- `MINOR`：向后兼容的新功能、新配置项或新公开 API。
- `MAJOR`：不兼容的 API、配置格式、存档格式或游戏行为变化。
- 同一提交包含多种变化时，采用影响最大的级别；不要为了保持旧编号而降低应有的版本级别。
- 默认分支只要会发布新 JAR，就必须使用尚未发布的版本号。纯本地检查且不提交时不修改版本。

修改版本时必须同步检查并更新：

1. 根 `pom.xml`；
2. `bedwars-api/pom.xml`；
3. `bedwars-plugin/pom.xml`；
4. `versionsupport_common/pom.xml`；
5. `versionsupport_v1_21_R3/pom.xml`；
6. `docs/zh_CN/api-development.md` 中的当前依赖示例；
7. `CHANGELOG.md` 顶部的中文更新记录。

## 队伍人数不变量

- `minPlayers` 表示整个竞技场进入正常开局倒计时所需的最低玩家数，`maxInTeam` 表示每支队伍的容量；不得再把 `minPlayers` 解释为每队下限。旧 `minInTeam` 只作为兼容入口迁移，不参与匹配。
- 正常匹配必须同时满足：总人数不少于 `minPlayers`、地图至少配置两支队伍、最终产生至少两支非空队伍、每支队伍不超过 `maxInTeam`，且所有玩家均能装入地图总容量。只有明确的 `/bw start debug` 可以允许单队测试。
- 达到开局人数后，分队应在满足至少两支非空队伍的前提下，尽量把玩家集中到最少的已启用队伍并优先填满已有队伍；仅在容量或完整小队边界要求时才启用更多配置队伍。正常大小的小队不可为了制造对手或集中人数而拆散，只有人数已经超过 `maxInTeam`、本来就不可能进入单队的外部大队伍才可拆分。
- 开局资格与最终分配必须根据当前人数、小队、选队偏好、地图队伍数和 `maxInTeam` 实时计算；不得为已知配置硬编码结果，也不得使用预生成或记忆化状态表判断是否合法。
- 示例：`minPlayers: 2`、`maxInTeam: 4` 时，两名未组队玩家应分到两队并开始倒计时；一个两人小队必须等待至少一名对手。`minPlayers: 6` 时，少于 6 人不能开始，6 人不要求每队达到 3 人；通常会按 `4+2` 集中到两队，只要求至少两队且均未超员。
- 修改开局资格或分队逻辑时，必须同时覆盖资格策略、实际分配、小队边界和倒计时结束后的最终校验。
- 等待人数达到竞技场最大人数且开局倒计时仍大于 5 秒时，必须直接压缩为固定 5 秒；判定应覆盖任务刚创建但尚未运行的首 tick，不新增周期扫描或可变配置项。

## 实现与验证

- 兼容旧配置：新增、移动或废弃配置键时必须通过迁移器更新，保留管理员自定义值，并按最新版模板所在配置节排序，不能简单追加到文件末尾。
- 错误修复写入 `BUGS.md`；所有发布写入 `CHANGELOG.md`。用户可见文档、配置注释和内置文字使用中文。
- 优先使用事件驱动或一次性初始化，避免没有必要的逐 tick 或周期性全量扫描。
- 新功能、修复和关键边界必须有回归测试。发布前执行：

```bash
mvn -B -ntp clean verify
```

- 构建完成后确认只有一个可发布插件 JAR，并从成品内部核对 `plugin.yml` 的版本与 POM 一致。
