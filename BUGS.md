# BUG 记录

本文档用于记录已经确认的问题、修复状态和复现依据。新问题应先记录，再进入修复和发布流程。

## 状态说明

- `待确认`：已有报告，但信息不足或尚未稳定复现。
- `已确认`：已经定位到具体代码或稳定复现步骤。
- `修复中`：已经开始修改，尚未通过完整验证。
- `待发布`：修改和测试已完成，但尚未进入正式构建。
- `已修复`：修复已进入标注版本并通过构建验证。
- `不会修复`：不受支持的环境或行为，需在备注中说明原因。

## 当前未解决问题

当前分支没有已确认但尚未修复的问题。

## 已修复问题

| 编号 | 发现版本 | 修复版本 | 问题 | 根因与处理 | 修复提交 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-142 | 10.0.0 | 10.0.1 | 玩家死亡后、重生事件触发前床被破坏时，死亡瞬间床仍存在的玩家会被错误淘汰 | 复活资格只在延后的 `PlayerRespawnEvent` 读取床状态，丢失了死亡瞬间的事实；现在在死亡处理时保存床完整状态，并在重生处理时消费该快照。倒计时期间的毁床不再改变本次死亡的复活资格，断线会清理快照 | 10.0.1 | 已修复 |
| BUG-141 | 9.0.3 | 9.0.4 | TAB 周期权威刷新在同一 tick 处理全部查看者，多人场景可能形成主线程和网络峰值 | 旧任务同步遍历所有 Sidebar 并为每个查看者重放竞技场玩家行；现在每 tick 最多处理四个查看者，保留客户端自愈能力并摊平峰值 | 9.0.4 | 已修复 |
| BUG-140 | 9.0.3 | 9.0.4 | 极端火球配置可能在重合击退修复后仍溢出为无穷速度，或使用过大半径扫描大量实体 | 配置值只做非负处理，有限大数在乘法后仍可溢出；现在加载时限制爆炸半径和击退强度，并在计算入口再次限幅 | 9.0.4 | 已修复 |
| BUG-139 | 9.0.2 | 9.0.3 | 火球命中时可能抛出 `IllegalArgumentException: x not finite`，导致 `ProjectileHitEvent` 处理失败 | 玩家坐标与火球爆炸坐标完全重合时，旧实现对零长度向量调用 `normalize()`，产生 `NaN` 分量并传给 Paper `setVelocity`。现在重合或非有限距离使用纯向上的有限击退，水平和垂直强度也统一清理为非负有限值，任何边界都不会向 Bukkit 传入 `NaN` 或无穷速度 | 9.0.3 | 已修复 |
| BUG-138 | 9.0.1 | 9.0.3 | TAB 玩家名在生产客户端仍可能保持白色，无法按队伍颜色区分 | 虽然插件发送了带队伍颜色的 `PlayerInfo UPDATE_DISPLAY_NAME`，Paper 1.21.11 生产客户端仍未稳定采用该名称颜色；同时玩家行解析器会主动删除语言模板中的 `{teamName}` 与 `{teamLetter}`，使管理员配置的降级前缀也失效。现在所有内置语言的竞技场队伍行显示 `{teamColor}[{teamLetter}] ` 前缀，旧内置空值自动迁移且管理员自定义值保留；模板中的队伍占位符恢复正常解析，因此名称即使仍为白色也能通过可见前缀区分队伍 | 9.0.3 | 已修复 |
| BUG-137 | 9.0.1 | 9.0.2 | 蠹虫不能稳定选择敌方玩家，有时会追逐或尝试攻击己方玩家 | 版本层每秒才把召唤物目标改成最近敌人，而原版蠹虫目标选择器会在两次刷新之间重新锁定最近玩家；旧监听器只取消友方目标，没有同步改选合法敌人，导致蠹虫反复浪费追击和攻击周期。现在蠹虫、铁傀儡的生成选敌、目标事件与低频失效兜底共用同一判定：只接受进行中对局内、同世界、存活、未复活等待、非旁观且不属于召唤队伍的玩家；无效目标会立即替换为最近敌人，合法当前目标不会被周期任务覆盖 | 9.0.2 | 已修复 |
| BUG-136 | 9.0.0 | 9.0.1 | TAB 玩家名仍可能显示为白色，加入或淘汰后玩家自己的行还可能保留旧队伍色 | 现有实现把逐查看者 `PlayerInfo displayName` 清空并假定 1.21.11 客户端一定使用 scoreboard Team 的颜色、前后缀重新渲染；实际生产客户端的该回退链仍不可靠。竞技场定向刷新又跳过了目标玩家自己的 Sidebar，导致本人行无法及时重建。现在按查看者直接发送“前缀 + 明确队伍色玩家名 + 后缀”，缓存完整渲染文本并在格式或颜色变化时更新；scoreboard Team 仅独立负责头顶名牌、碰撞和隐身可见性。加入、重连与淘汰会刷新包括本人在内的全部同场查看者，同时继续禁止对本人行伪造观察者游戏模式 | 9.0.1 | 已修复 |
| BUG-135 | 8.1.0 | 9.0.0 | 玩家隐身后主副手物品仍然可见，换手或换装可能重新暴露装备；报警陷阱移除药效后还可能继续隐藏装备 | 版本支持层只发送四件护甲的空装备包，各入口又分别维护 `showTime`、药水和查看者规则；库存变化、受击、自然到期与陷阱清理没有统一状态所有者。现在六个装备槽共用同一数据包映射，Paper 装备变化事件按玩家合并到下一 tick 重同步；同队显示真实装备，敌队和旁观者隐藏。合法敌方伤害、药效到期、陷阱、复活、离场和结算均通过统一管理器原子清理计时、药效、名牌事件和装备显示 | 9.0.0 | 已修复 |
| BUG-134 | 7.1.1 | 8.0.0 | 潜行发射火球看不出 1.5 倍初速度，持续加速度、后坐力和射速也低于预期 | Paper 1.21.11 的 Bukkit `Fireball#setAcceleration(Vector)` 名称具有误导性，实际会重设当前速度；发射链路先构造 1.6/2.4 初速度，随后又把两者覆盖为 0.1。现在版本支持层直接设置 NMS `accelerationPower`，保留真实初速度；潜行持续加速度提高到 0.2，后坐改为 0.10 的完整三维反方向，默认冷却降至 0.4 秒。固定 Paper 运行探针同时验证该字段契约 | 8.0.0 | 已修复 |
| BUG-133 | 7.1.0 | 7.1.1 | 旁观者落地时可能“卡脚”，客户端认为自己是观察者模式，但服务器和反作弊仍认为是冒险模式 | TAB 观察者样式对所有查看者发送 `UPDATE_GAME_MODE: SPECTATOR`，没有排除目标就是查看者本人的 PlayerInfo 行；1.21.11 客户端会把本人行的模式更新应用到本地移动逻辑，但服务端 Bukkit 模式没有改变。现在本人行永远保留 Paper 真实模式，并恢复旧缓存；其他查看者仍看到观察者样式。重连流程同时删除独立的延迟 `SURVIVAL + flying` 写入，只由竞技场重生会话管理真实模式和飞行 | 7.1.1 | 已修复 |
| BUG-132 | 7.1.0 | 7.1.1 | 游戏结束后返回物品可能不存在或无法右键使用，超时结算尤其容易复现 | 获胜者只被清空背包却没有获得结算物品；死亡等待和第一人称观战还可能保留真实 `SPECTATOR`，该模式不能正常使用物品。旧处理若挂在 `GameEndEvent` 又会漏掉无唯一胜者的超时路径。现在进入 `restarting` 后、TAB 重建前统一结束重生会话、恢复 `ADVENTURE` 和正确飞行状态，再向全部在线参与者发放旁观物品；结束事件只负责清理掉落物 | 7.1.1 | 已修复 |
| BUG-131 | 7.1.0 | 7.1.1 | 游戏结束后侧边栏直接显示 `{topPlayerDisplayName}`、`{topTeamColor}`、`{topValue}` 或无胜者队伍占位符 | 结算统计对象只在游戏结束聊天排行榜循环末尾附加；关闭或清空聊天排行榜会提前 `continue`，侧边栏重建时没有解析上下文。现在侧边栏在进入 `restarting` 时独立创建并持有统计快照，与聊天配置解耦；无胜者占位符也有明确回退，退出结算状态后清理快照 | 7.1.1 | 已修复 |
| BUG-130 | 7.0.0 | 7.1.0 | TAB 玩家名前重复显示队伍名称/首字母，淘汰者和纯旁观者也没有原版观察者模式样式 | 内置语言仍把 `{teamName}`/`{teamLetter}` 写入玩家行前缀，而 PlayerInfo 只管理 nullable 显示名，没有按查看者维护游戏模式。现在玩家名仅由 scoreboard Team 着队伍色并维持光谱队序、同队聚合和字典序；玩家行运行时保证队名/首字母占位符为空，磁盘自定义模板的其他内容保持不变。淘汰者与纯旁观者按查看者发送 `UPDATE_GAME_MODE: SPECTATOR`，不修改真实 Bukkit 模式，退出时恢复 Paper 当前模式与第三方名称 | 7.1.0 | 已修复 |
| BUG-129 | 7.0.0 | 7.1.0 | 玩家被反作弊封禁或服务器踢出后，最后一名对手有时永远无法结束游戏 | 重连成功时没有清除 `leaving` 中按 UUID 相等的旧 Player；再次 Quit 会被防重逻辑直接跳过，离线成员永久留在队伍。所有 Quit 又一律创建重连占位，没有区分最终踢出。现在成功重连前按 UUID 结束旧离场生命周期，`QuitReason.KICKED` 在正常移除后立即按放弃比赛清除当前玩家占位并检查胜负；普通断线/超时继续允许重连，且只有在线成员为空并且没有其他同队有效重连占位时才淘汰该队 | 7.1.0 | 已修复 |
| BUG-128 | 7.0.0 | 7.1.0 | 部分玩家放置方块会永久无法破坏，或一次取消/失败的破坏后失去玩家方块所有权 | 破坏与爆炸保护先判断地图保护区，再判断玩家方块；所有权监听又在 MONITOR 事件中立即删除记录，没有核对后续监听器和下一 tick 的真实方块结果。现在玩家方块所有权优先于普通地图保护，队伍床及 NPC/生成器专用保护保持不变；放置、破坏、爆炸、燃烧、消退、Paper 连锁破坏和下落方块均保存快照并在下一 tick 按已加载区块内的最终状态校正，未实际销毁的方块继续保留所有权 | 7.1.0 | 已修复 |
| BUG-127 | 6.0.0 | 7.0.0 | PlaceholderAPI 的 `%bw1058_elapsed_time%` 在超过 59 秒后显示无效时间，例如 65 秒显示为 `01:65` | 旧实现直接输出 `Duration#toMinutes()` 和 `toSeconds()` 的累计总数，没有把分钟和秒限制到当前时钟分量。现在 TAB 新增的 `{gameTime}` 与 PAPI 共用一个纯格式化器，分钟和秒正确取模，负时长钳到零，缺少开始时间返回空文本 | 7.0.0 | 已修复 |
| BUG-126 | 5.0.0 | 6.0.0 | BedWars 世界初始化后时间仍会自然流逝，主大厅也可能保持夜晚或雨天，不能稳定维持最高自然天光 | Paper 1.21.11 把旧 `doDaylightCycle`、`doWeatherCycle`、`doMobSpawning` 注册键改为 `advance_time`、`advance_weather`、`spawn_mobs`，旧代码按字符串查找时得到空值却静默跳过；同时世界范围判断只包含竞技场、加载队列和设置会话，不包含配置大厅。现在内部直接使用 1.21.11 现代 GameRule 常量，旧竞技场规则名显式映射到新键；专用模式覆盖全部世界，SHARED 仅覆盖 BedWars 竞技场、加载队列、设置世界和配置大厅，并用时间、天气及游戏规则变化事件持续守住正午与晴天 | 6.0.0 | 已修复 |
| BUG-125 | 4.0.8 | 5.0.0 | 玩家被火球直接炸死时，死亡提示没有显示实际发射者 | 火球碰撞监听器虽然更新了通用最后一击，但用无来源的 `damage(double)` 结算伤害，Paper 将死亡原因记为 `CUSTOM`；死亡处理只在爆炸分支读取来源，因而退回未知死因。现在敌方伤害使用 direct 为火球、causing 为发射者的 `PLAYER_EXPLOSION` DamageSource，死亡解析优先读取当前伤害源，只有无来源爆炸才回退最新一击；同步标记只放行插件定额伤害并继续取消原生实体爆炸，避免重复结算，自己和队友使用无归属爆炸源 | 5.0.0 | 已修复 |
| BUG-124 | 4.0.7 | 4.0.8 | 大厅里的代理返回物品可能变成默认红床、等待区旧物品或同槽位的其他物品；进入大厅后还可能延迟清空整个背包 | 旧架构 15 曾直接删除自定义 `lobby-items.leave`，架构 18 又补入默认值，后续迁移继续无版本条件重建节点；正常运行时固定延迟 15 tick，只校验世界后清空背包，跨世界入口又没有应用大厅状态，配置写槽位还依赖 YAML 后写覆盖。现在从架构 15 删除前的最后快照，或架构 15–17 中重新配置过的最新快照，有条件恢复自定义节点，并用架构 18 后的删除或改写快照否决旧值；大厅入口立即刷新并在 15 tick 后按实时上下文选择性复核，只替换 BedWars 命令物品，跨世界入口统一补齐，坏项隔离且代理返回项在槽位冲突中具有稳定优先级；旧公开 API 的全背包清空契约保持不变 | 4.0.8 | 已修复 |
| BUG-123 | 4.0.6 | 4.0.7 | 玩家死亡或最终淘汰时可能出现明显单 tick 卡顿，损坏地图附近还可能同时触发区域文件读取 | 死亡链路把旧队伍状态的全量侧边栏重建、逐槽位完整背包同步、安全出生点方块扫描、可见性 TAB 重放和场外指示器更新集中在相邻 tick；告示牌注册和墙牌支撑块写入还可能触发未加载区块。现在淘汰刷新按竞技场合并并增量写入且拒绝过期玩家，复活附魔至多同步一次背包，出生点与告示牌只访问已加载区块，告示牌按区块索引补写，场外指示器延后合并；地图自身已损坏的数据仍须从备份恢复 | 4.0.7 | 已修复 |
| BUG-122 | 4.0.5 | 4.0.6 | SHARED 模式下玩家从非大厅世界进入配置大厅后，TAB 仍保持登录时的空白/原版行；反向离开也会残留大厅行 | 两边的竞技场上下文都为 `null`，跨世界路径只标记等待 `PlayerClientLoadedWorldEvent` 后重放旧 scoreboard；Paper 1.21.11 普通跨世界并不会再次触发该事件。现在 `PlayerChangedWorldEvent` 下一 tick 主动消费标记，先按实时世界重建 TAB 行再重新绑定；若登录/重生事件已经消费则自动去重 | 4.0.6 | 已修复 |
| BUG-121 | 4.0.5 | 4.0.6 | 默认关闭 waiting/starting 完整 TAB 格式时，预选队伍已经着色但玩家仍未按同队、字典序和光谱队序排列 | 全局排序入口仍用外观配置过滤整个竞技场，与 4.0.5 已建立的最小队伍色上下文不一致；现在所有活动竞技场均参与排序，配置只控制可选前后缀 | 4.0.6 | 已修复 |
| BUG-120 | 4.0.4 | 4.0.5 | 其他插件替换玩家 scoreboard 后，SimpMC-BedWars 周期刷新仍执行但 TAB 和头顶队伍色一直是白色 | 刷新只更新内部缓存的旧 scoreboard，同时继续发送 PlayerInfo 空名称；玩家实际绑定的外部面板没有 `bw_t_*` Team，客户端只能回退原版白名。现在每次 TAB 更新和强制重放先校验面板所有权，漂移时保存外部面板并重新绑定完整受管快照 | 4.0.5 | 已修复 |
| BUG-119 | 4.0.4 | 4.0.5 | 玩家快速离开竞技场 A 并进入 B 后，A 的延迟回大厅任务会删除 B 的 TAB 与头顶队伍色 | 玩家和旁观者离场分别留下 5 tick/下一 tick 回调，执行时未检查实时竞技场且直接传入 `null`；现在回调仅在玩家在线、未断线且仍未分配竞技场时运行，Sidebar 入口也以实时注册表纠正过期参数 | 4.0.5 | 已修复 |
| BUG-118 | 4.0.4 | 4.0.5 | 关闭竞技场侧边栏、页首页尾和当前状态完整 TAB 格式后，TAB 与头顶名牌都会失去队伍颜色 | 竞技场是否创建 TAB 上下文错误地由外观开关共同决定，全部关闭时根本没有为观察者绑定包含队伍成员的 scoreboard；现在只要玩家位于竞技场就保留最小队伍色上下文，外观开关只控制可选内容 | 4.0.5 | 已修复 |
| BUG-117 | 4.0.4 | 4.0.5 | 后续 `ADD_PLAYER` 或可见性重建可能再次把生产客户端的 TAB 队伍色覆盖成白色 | 4.0.4 发送非空完整显示名，客户端把它作为独立渲染来源并与头顶 Team 状态分叉；现在只对 BedWars 查看者发送 nullable 空值，由 scoreboard Team 统一渲染，并在登录、跨世界、`showPlayer` 与强制刷新后重放。目标玩家的全局 `playerListName` 保持不变，退出时按 Paper 当前状态恢复 | 4.0.5 | 已修复 |
| BUG-116 | 4.0.4 | 4.0.5 | 等待或倒计时阶段已经通过 GUI 选择队伍，TAB 仍显示白名且没有按所选队伍聚合 | 预选只保存在 `PreGameTeamSelectionManager`，TAB 却仅查询开局结束后才写入的正式队伍，选队后也没有刷新事件；现在等待阶段统一解析正式队伍、预选队伍和历史队伍，选择或取消后立即刷新同一竞技场所有观察者中的受影响小队 | 4.0.5 | 已修复 |
| BUG-115 | 4.0.3 | 4.0.4 | 使用同一 TAB 标识符替换玩家后，旧玩家的头顶名牌仍继承新玩家的队伍格式 | 替换路径只恢复旧玩家的 PlayerInfo 名称并向既有 scoreboard team 添加新玩家，没有删除旧 entry；现在在替换前从每个受管 team 删除旧玩家名，再统一应用新成员、颜色和可见性 | 4.0.4 | 已修复 |
| BUG-114 | 4.0.3 | 4.0.4 | 附属插件为同一观察者叠加多个 Sidebar 时，释放新实例不能恢复上一层 TAB 名称，非顺序移除还可能恢复失效 scoreboard | 所有权表只保留最后一个实例，覆盖时没有撤销旧 PlayerInfo 名称，也没有维护 scoreboard 回退链；现在使用后进先出所有权链，接管、释放、非顺序删除和重新挂载都会同步修复名称及面板回退关系 | 4.0.4 | 已修复 |
| BUG-113 | 4.0.3 | 4.0.4 | 移除 TAB 行时若恢复数据包发送失败，客户端会一直保留过期颜色或前后缀 | 行状态先被删除，失败后只留下文本缓存而没有可重试的玩家目标；现在按观察者保留去重的待恢复目标，后续明确刷新先重试，成功、离线或被新状态覆盖后再释放 | 4.0.4 | 已修复 |
| BUG-112 | 4.0.3 | 4.0.4 | `BUG-111` 清理玩家对象后仍保留每个历史 TAB 标识符与内部队伍名 | `removeTab` 通过 `computeIfAbsent` 获取队伍名且从不删除映射，`removeTabs` 也没有清空；现在单行删除直接移除已有映射，批量清理释放全部映射并安全重置单调分配器 | 4.0.4 | 已修复 |
| BUG-111 | 4.0.2 | 4.0.3 | 长期运行的大厅会在每个观察者的 TAB 状态中无界保留已退出玩家 | 玩家退出服务器或离开竞技场时只移除了其自己的 Sidebar，没有从其他观察者的已部署 TAB 行和逐观察者显示名缓存中删除该目标；周期刷新还会继续遍历这些历史对象。现在加入、离场和退出事件都会先清理所有观察者中的旧目标行，离线目标无需发送无效恢复包即可释放缓存 | 4.0.3 | 已修复 |
| BUG-110 | 4.0.2 | 4.0.3 | 插件卸载时大厅玩家可能残留 TAB 页首页尾、列表顺序或受管 scoreboard | 竞技场销毁只清理属于该竞技场的 Sidebar，一直留在大厅的实例没有统一关闭；列表顺序恢复还依赖下一 tick 任务，而 Paper 会在卸载后取消该任务。现在监听本插件禁用事件，同步清理全部 Sidebar、延迟状态和列表顺序，并逐玩家隔离清理异常 | 4.0.3 | 已修复 |
| BUG-109 | 4.0.1 | 4.0.3 | 玩家进入服务器时 TAB 保持原版样式，加入竞技场后才开始更新 | 4.0.2 仍可能让首次世界加载事件早于异步大厅 TAB 初始化，并且在 `PlayerJoinEvent` 内立即更新已有观察者时，显示名包会早于服务端随后发送的 `ADD_PLAYER`，客户端直接丢弃未知 UUID 的更新。现在首次加载缺少侧边栏时立即创建，向已有观察者更新新玩家则延后一 tick，确保 PlayerInfo 条目已经存在 | 4.0.3 | 已修复 |
| BUG-108 | 3.0.1 | 4.0.3 | 服务端已经设置 scoreboard team 颜色，但生产客户端的 TAB 玩家名仍可能保持白色 | 4.0.2 仍假定清空全局 `playerListName` 后客户端必然套用 scoreboard team；实际 1.21.11 只要 PlayerInfo 的 `displayName` 非空，就完全跳过 team 的颜色、前缀和后缀。现在按查看者直接发送包含明确队伍色的完整显示名，`showPlayer` 重建条目后只重放对应目标行；重连更新等待原队伍恢复后执行，scoreboard team 仅独立负责头顶名牌及可见性 | 4.0.3 | 已修复 |
| BUG-107 | 3.0.1 | 4.0.0 | 设置会话结束时可能压缩仍加载的竞技场世界，存在把不一致区域文件写入恢复缓存的风险 | `onSetupSessionClose` 在异步传送玩家之前卸载世界，且忽略卸载失败结果后直接异步压缩；现在等待世界无人、同步保存并确认卸载成功后才压缩，超时则放弃写缓存，缓存 ZIP 也改为临时文件完成后原子替换 | 4.0.0 | 已修复 |
| BUG-106 | 3.0.1 | 4.0.0 | 大厅玩家离开服务器时可能没有退出提示，且旧缓存状态可能把非大厅离开误报给大厅 | 退出广播位于竞技场和语言清理之后，前序失败会吞掉提示，判断还接受过期 presence 缓存；现在在清理前按事件发生时的真实大厅上下文广播，并只向当前大厅玩家发送 | 4.0.0 | 已修复 |
| BUG-105 | 3.0.1 | 4.0.0 | 正常开局错误地按每队 `minInTeam` 判断，不能恢复 BedWars1058 的全场 `minPlayers + maxInTeam` 行为 | 2.10.x 后续多次修改把竞技场最低开局人数替换成每队下限；现在恢复全场 `minPlayers`，统一资格和最终分配校验，同时保留双队、容量、小队完整与均衡分散约束 | 4.0.0 | 已修复 |
| BUG-104 | 3.0.1 | 4.0.0 | 竞技场内 TAB 仍只有 BedWars1058 的 104 空格宽度，与已拓宽的大厅不一致 | 宽度补齐只在 `hasNoArena()` 分支执行；现在所有竞技场状态和大厅共用至少 128 空格的页首规范化 | 4.0.0 | 已修复 |
| BUG-103 | 3.0.0 | 3.0.1 | TAB 玩家名没有可靠地显示为队伍颜色，也无法确认头顶名牌是否独立生效 | 3.0.0 只清除了自定义 `playerListName` 并依赖客户端继续套用 scoreboard 队伍样式，测试也只检查了 setter 调用；关闭完整 TAB 格式时还会一起删除负责头顶名牌的队伍。现在显式渲染完整 TAB 名称组件，scoreboard team 独立负责头顶颜色，并在关闭可选前后缀时保留最小队伍色格式 | 3.0.1 | 已修复 |
| BUG-102 | 2.13.1 | 3.0.0 | 火球飞行速度不足，按住潜行投掷时额外加速也不明显 | Paper 1.21.11 的 `Fireball#getDirection()` 实际返回加速度，旧代码却再次把它当单位方向计算初速度；现在直接按瞄准方向构造速度向量，普通初速度提高到 1.6 格/tick，潜行输入明确提高到 2.4 格/tick，同时保留原版 0.1 加速度避免客户端预测抖动 | [6882f0c6](https://github.com/Minecraft0122/SimpMC-BedWars/commit/6882f0c687940441cc38cd54a4fe0ca25e08c936) | 已修复 |
| BUG-101 | 2.13.1 | 3.0.0 | 玩家转头或轻微移动时仍反复执行区块、虚空、床悬浮字和基地检测，资源生成时还扫描无关实体，高在线服务器会产生不必要的主线程负载 | 两个移动监听器没有使用 Paper 的位置/方块变化标志，队伍查询又反复线性遍历；生成器和火球使用宽泛实体查询。现在按位置、方块和区块变化分层短路，开局后以 UUID 常数时间定位队伍，并使用 Paper 1.21.11 类型化邻近查询与合并掉落实体 | [6882f0c6](https://github.com/Minecraft0122/SimpMC-BedWars/commit/6882f0c687940441cc38cd54a4fe0ca25e08c936) | 已修复 |
| BUG-100 | 2.13.1 | 3.0.0 | 锋利虽然能购买到 IV，但默认价格 4、8、16、32 每级翻倍，不符合约 1.5 倍的分级要求 | 默认价格仍沿用二次幂公式；现在从 4 钻石开始按 1.5 倍逐级四舍五入为 4、6、9、14，并仅迁移仍等于旧默认值的配置 | [6882f0c6](https://github.com/Minecraft0122/SimpMC-BedWars/commit/6882f0c687940441cc38cd54a4fe0ca25e08c936) | 已修复 |
| BUG-099 | 2.13.1 | 3.0.0 | 地图列表在多个竞技场均处于重置状态时可能排序失败 | 旧比较器对两个 `restarting` 对象无论参数顺序都返回正数，违反比较器契约；现在按状态优先级、人数和名称组成稳定且可传递的比较器 | [6882f0c6](https://github.com/Minecraft0122/SimpMC-BedWars/commit/6882f0c687940441cc38cd54a4fe0ca25e08c936) | 已修复 |
| BUG-098 | 2.13.1 | 3.0.0 | `joinRandomArena` 没有加入任何地图时仍向附属插件返回成功 | 循环结束后无条件返回 `true`；现在只在 `addPlayer` 成功时返回 `true`，没有可加入地图时返回 `false` | [6882f0c6](https://github.com/Minecraft0122/SimpMC-BedWars/commit/6882f0c687940441cc38cd54a4fe0ca25e08c936) | 已修复 |
| BUG-097 | 2.13.1 | 3.0.0 | 玩家最终死亡进入旁观时会出现明显的主线程卡顿 | 淘汰发生在 `PlayerRespawnEvent` 内，却同步重建计分板、刷新同场全部 TAB，并遍历全服可见性和全部悬浮字；现在事件只切换必要状态，下一 tick 只同步同场玩家且不重建客户端仍持有的悬浮字，主动加入观战时才执行完整初始化 | [6882f0c6](https://github.com/Minecraft0122/SimpMC-BedWars/commit/6882f0c687940441cc38cd54a4fe0ca25e08c936) | 已修复 |
| BUG-096 | 2.13.0 | 2.13.1 | TAB 仍未按队伍颜色渲染玩家名，队伍顺序不符合光谱顺序，淘汰状态也可能延迟 | 先前修复把上游状态化队伍前缀强制替换为空，中文迁移还主动清除了旧前缀；排序直接沿用竞技场配置顺序且把 1.21 客户端的高值优先方向写反，最终淘汰又只重建玩家本人的计分板。现在恢复上游前缀和 Paper 队伍颜色，由 RGB 色相计算队伍顺序，同队成员统一按名字排序，并在淘汰后事件驱动地刷新所有查看者 | 2.13.1 | 已修复 |
| BUG-001 | 2.1.0 | 2.1.1 | 玩家加入时解析旧大厅坐标发生数组越界 | 旧配置可能只有五个字段；位置解析器和配置迁移现在会补全、规范化并备份旧配置 | [fef65e52](https://github.com/Minecraft0122/SimpMC-BedWars/commit/fef65e52) | 已修复 |
| BUG-002 | 2.3.0 | 2.4.0 | 出生位置被方块遮挡时可能卡住或窒息 | 增加附近安全位置搜索，并允许一格高空间使用趴下姿态 | [ce4549a7](https://github.com/Minecraft0122/SimpMC-BedWars/commit/ce4549a7) | 已修复 |
| BUG-003 | 2.4.0 | 2.5.0 | 从竞技场设置模式返回大厅后模式和大厅物品不正确 | 返回大厅流程没有统一恢复玩家状态；现在传送成功后重新应用大厅状态和物品 | [bec32cef](https://github.com/Minecraft0122/SimpMC-BedWars/commit/bec32cef) | 已修复 |
| BUG-004 | 2.6.0 | 2.8.0 | 竞技场结束后多个定时任务持续抛出 NPE | 销毁方法把玩家等集合设为 `null`，但侧边栏及奖励任务仍持有竞技场；现在先注销任务和侧边栏，再安全清空非空集合 | [b4ced4b8](https://github.com/Minecraft0122/SimpMC-BedWars/commit/b4ced4b8) | 已修复 |
| BUG-005 | 2.7.0 | 2.8.0 | 死亡时旁观模式偶尔没有成功切换 | 旧流程包含多次模式校验、隐藏和回退状态；现在根据死亡类型设置位置后只切换一次旁观模式 | [b4ced4b8](https://github.com/Minecraft0122/SimpMC-BedWars/commit/b4ced4b8) | 已修复 |
| BUG-006 | 2.7.0 | 2.8.0 | 死亡和复活会执行重复的玩家隐藏、显示及出生同步 | 删除全场隐藏、重复显示循环和延迟重复同步，只保留一次必要的复活同步 | [b4ced4b8](https://github.com/Minecraft0122/SimpMC-BedWars/commit/b4ced4b8) | 已修复 |
| BUG-007 | 2.8.0 | 2.9.0 | 玩家进入大厅后被设置为生存模式 | 大厅物品刷新硬编码了生存模式；现在将“进入大厅状态”和“刷新物品”分离，只在实际进入时设置冒险模式 | 2.9.0 | 已修复 |
| BUG-008 | 2.8.0 | 2.9.0 | 部分配置下游戏可能只有一支非空队伍 | 最小人数允许设为 1，且完整邀请组可能独占一队；现在自动迁移最小人数并在分配及开局阶段校验两支非空队伍 | 2.9.0 | 已修复 |
| BUG-009 | 2.9.0 | 2.10.0 | 控制台反复报告 `zh_cn.v1.bak`、`zh_cn.v2.bak` 缺少 `generator-timer` | 语言扫描使用“包含 `.yml`”判断，误加载了配置迁移备份；现在只匹配规范语言文件名并忽略所有 `.bak` 文件 | 2.10.0 | 已修复 |
| BUG-010 | 2.10.2 | 2.10.3 | 玩家放置方块与地图原生方块可能混淆 | 可重复列表、自动点燃 TNT 和爆炸清理遗漏会留下幽灵坐标；现在使用唯一坐标索引，并同步所有成功放置、爆炸、燃烧、褪色和重力方块事件 | 2.10.3 | 已修复 |
| BUG-011 | 2.10.3 | 2.10.4 | 受保护商店物品可通过背包拖拽或副手交换进入外部容器 | 原监听器只处理点击和数字键，且重复分支对物品来源判断不一致；现在按移动来源统一判断，并覆盖拖拽、数字键和副手交换 | 2.10.4 | 已修复 |
| BUG-012 | 2.10.4 | 2.10.5 | 同一火球影响多名玩家时后续玩家击退方向不稳定 | 击退循环反复修改爆炸位置向量；现在每名玩家使用独立克隆向量计算，并增加方向回归测试 | 2.10.5 | 已修复 |
| BUG-013 | 2.10.4 | 2.10.5 | 简体中文 TAB 过宽且大量状态仍显示英文 | 内置模板用约 100 个空格撑宽界面并残留英文，开局旁观前后缀还错误复用了玩家路径；现在使用紧凑中文模板并自动迁移旧默认值 | 2.10.5 | 已修复 |
| BUG-014 | 2.10.4 | 2.10.5 | 重置阶段 TAB 的 `{time}` 不显示真实重置剩余时间 | 重置状态错误读取下一游戏事件计时；现在直接读取 `RestartingTask`，并同步到每秒操作栏提示 | 2.10.5 | 已修复 |
| BUG-015 | 2.10.5 | 2.10.6 | 已安装 Vault 时仍可能无法识别经济功能，且商店扣款失败 | 旧实现要求插件名恰好为 `Vault`、只检查一次服务，并错误调用银行账户扣款；现在监听 Bukkit 服务注册表变化并使用玩家账户扣款 | 2.10.6 | 已修复 |
| BUG-016 | 2.10.6 | 2.10.7 | 资源生成器实际刷新周期始终比 `delay` 配置多 1 秒 | 旧倒计时只在剩余值已经变为 0 后的下一次任务才生成；现在剩余 1 秒时按期生成，并把 0 或负数配置安全限制为 1 秒 | 2.10.7 | 已修复 |
| BUG-017 | 2.10.7 | 2.10.8 | 正常关机卸载竞技场被输出为 `WARN` | 卸载流程错误使用 WARNING 日志级别；现在通过 Paper 组件日志输出黄色 INFO，颜色不可用时退化为普通 INFO | 2.10.8 | 已修复 |
| BUG-018 | 2.10.8 | 2.10.9 | 多支队伍的岛屿保护半径重叠时，箱子归属取决于队伍遍历顺序 | 旧逻辑把半径内最后遍历到的队伍视为箱子主人；现在固定选择出生点距离箱子最近的队伍，并安全排除不同世界 | 2.10.9 | 已修复 |
| BUG-019 | 2.10.9 | 2.10.9 | 首次自动发布任务构建成功但无法创建 Release | 独立发布任务没有检出源码，GitHub CLI 无法从 `.git` 推断仓库；现在通过 `GH_REPO` 显式传入仓库标识，同时继续保持发布任务不执行仓库代码 | 2.10.9 | 已修复 |
| BUG-020 | 2.10.9 | 2.10.10 | 中文 TAB 在清理旧超宽模板后显示过窄 | 2.10.5 只删除了超长空格，没有设置合理下限；现在按去除颜色后的可见字符计算，仅为过窄模板补充 30 字符分隔线 | 2.10.10 | 已修复 |
| BUG-021 | 2.10.9 | 2.10.10 | 大厅 TAB 没有展示头尾或玩家格式 | TAB 生命周期错误依附右侧大厅计分板，关闭计分板或侧边栏为空时整个 TAB 上下文会被删除；现在支持无侧边栏目标的独立 TAB 上下文 | 2.10.10 | 已修复 |
| BUG-022 | 2.10.9 | 2.10.10 | TAB 中出现两个看起来不同的 ping | 其中一个实际是插件注册在玩家列表中的生命值数字，另一个才是原版延迟图标；现在默认只保留原版延迟，旧默认配置自动迁移 | 2.10.10 | 已修复 |
| BUG-023 | 2.10.9 | 2.10.10 | 部分大厅状态下玩家仍能丢弃背包物品 | 丢弃监听复用了建造会话例外且只识别 MULTIARENA；现在 MULTIARENA/SHARED 大厅始终取消玩家丢弃事件 | 2.10.10 | 已修复 |
| BUG-024 | 2.10.10 | 2.10.11 | 青色队伍被实现为淡蓝色，导致自动建队找不到青色羊毛且商店发放淡蓝色羊毛 | `AQUA` 的现代染料及全部方块材质错误映射到 `LIGHT_BLUE_*`；现在使用规范 `CYAN`/`CYAN_*` 映射并自动迁移旧竞技场配置 | 2.10.11 | 已修复 |
| BUG-025 | 2.10.10 | 2.10.12 | TAB 被压成带横线的窄条，原有菜单结构丢失 | 自动宽度补丁把分隔线混入状态模板；现在恢复独立的经典全局页首/页尾配置并删除横线补丁 | 2.10.12 | 已修复 |
| BUG-026 | 2.10.11 | 2.10.12 | 游戏中队友名字被特殊绿色覆盖，未完整使用所属队伍颜色 | 玩家列表颜色额外读取 `teammate-color`；现在活跃玩家统一直接使用队伍颜色，旧配置项自动删除 | 2.10.12 | 已修复 |
| BUG-027 | 2.10.11 | 2.10.12 | 重置倒计时只在 TAB/操作栏显示，且最后数秒因玩家提前离场无法收到 | 旧任务每秒写操作栏并在剩余 7 秒时移出玩家；现在按指定里程碑写入聊天栏，并在广播 0 后才移出玩家 | 2.10.12 | 已修复 |
| BUG-028 | 2.10.12 | 2.10.13 | 所谓“恢复经典 TAB”实际使用了新设计的全局固定页首页尾，并非原仓库 TAB | 误把经典理解为固定品牌标题；现在以 `andrei1058/BedWars1058@66987262` 为基准恢复全部状态化模板选择与原版布局 | 2.10.13 | 已修复 |
| BUG-029 | 2.10.14 | 2.10.15 | 队伍名称会被语言文件翻译，聊天中还会被强制转为大写 | `getDisplayName(Language)` 读取自动生成的 `team-name-*`，聊天又二次转换大小写；现在所有显示统一返回竞技场配置原名 | 2.10.15 | 已修复 |
| BUG-030 | 2.10.14 | 2.10.15 | 携带 TNT 的头顶红色粒子在向上搭建时遮挡本人视线 | 世界粒子广播把携带者本人也列为接收者；现在仅向 32 格内其他玩家定向发送，并把隐身分支的错误 `return` 改为 `continue` | 2.10.15 | 已修复 |
| BUG-031 | 2.10.13 | 2.10.16 | 等待阶段执行 `/bw upgradesmenu` 发生 `ITeam` 空指针异常 | 命令把“竞技场玩家”等同于“已分配队伍的游戏中玩家”；现在依次校验游戏状态、玩家身份、队伍、NPC 位置和菜单实例 | 2.10.16 | 已修复 |
| BUG-032 | 2.10.13 | 2.10.16 | 玩家死亡时基地监听器可能计算大厅与竞技场床之间的距离并抛出异常 | 竞技场映射与世界切换存在短暂不同步，旧代码直接调用 `Location.distance`；现在所有基地相关半径判断先校验世界并使用平方距离 | 2.10.16 | 已修复 |
| BUG-033 | 2.10.13 | 2.10.16 | 启动时报告 `Category material not set at: config-version` | 商店遍历所有 YAML 根键，误把配置迁移器的标量版本字段当成分类；现在只接受非保留的配置节 | 2.10.16 | 已修复 |
| BUG-034 | 2.10.13 | 2.10.16 | 没有可传送玩家时，旁观传送器尝试创建 0 格背包并使延迟命令任务报错 | GUI 大小直接由活跃玩家数取整，0 未设置下限；现在空目标直接关闭，并把非空分页限制为 9–54 格 | 2.10.16 | 已修复 |
| BUG-035 | 2.10.16 | 2.10.17 | 大厅坐标缺失或使用默认世界回退时仍能把背包物品扔出去 | 丢弃保护只比较缓存的大厅世界名称；现在同时识别未分配竞技场的非竞技场世界，并在取消丢弃后同步客户端背包 | 2.10.17 | 已修复 |
| BUG-036 | 2.10.16 | 2.10.17 | 主大厅持续发放没有实际作用的“回到主大厅”物品 | 默认配置和旧配置迁移器都会补回 `lobby-items.leave`；现在停止创建并在架构 15 迁移时自动删除，仅保留等待/旁观阶段的离开物品 | 2.10.17 | 已修复 |
| BUG-037 | 2.10.17 | 2.10.18 | NPC 位于方块中心时，1 格保护半径会漏掉负方向、对角及多方块放置 | 旧逻辑用方块整数角点和 NPC 的 x.5/z.5 中心计算欧氏距离，范围不对称；现在按方块坐标保护 NPC 身体周围一格，并检查多方块放置事件 | 2.10.18 | 已修复 |
| BUG-038 | 2.10.18 | 2.10.19 | 商店永久护甲固定只发放护腿和靴子，无法按服务器玩法选择完整护甲 | 默认 `shop.yml` 的三种护甲商品只声明两个下装槽位；现在安全补齐四件套，并由 `config.yml` 的全服开关决定实际发放下半身或全身 | 2.10.19 | 已修复 |
| BUG-039 | 2.10.18 | 2.10.19 | 游戏 TAB 无法稳定按队伍分组，队内玩家也未按名字典序排列 | 旧排序前缀最终被哈希为计分板队伍名，顺序信息丢失；现在使用 Paper 原生玩家列表顺序，按竞技场、队伍和玩家名稳定排序，并避免重复数据包 | 2.10.19 | 已修复 |
| BUG-040 | 2.10.20 | 2.10.21 | `/bw start debug` 创建倒计时后仍会被“两支非空队伍”校验终止 | 调试参数只创建了开局任务，没有把绕过意图传到最终分队校验；现在使用竞技场本局专属标记，仅调试开局允许恰好一支非空队伍 | 2.10.21 | 已修复 |
| BUG-041 | 2.10.17 | 2.10.22 | 主大厅第 9 格的“回到主大厅”红床被配置迁移主动删除 | 旧迁移把 `lobby-items.leave` 视为无用项；现在恢复默认红床，并只补齐缺失字段以保留管理员自定义值 | 2.10.22 | 已修复 |
| BUG-042 | 2.10.17 | 2.10.22 | OP 在主大厅仍无法移动、装备或丢弃背包物品 | 大厅保护与命令物品保护均未统一检查 OP；现在两个监听链路都放行大厅 OP，非 OP 继续受保护 | 2.10.22 | 已修复 |
| BUG-043 | 2.10.22 | 2.10.23 | 管理员设置竞技场时无法在目标地图放置方块 | 竞技场世界边界保护没有识别设置会话，把管理员当成未加入竞技场的普通玩家；现在只对玩家当前设置的目标世界放行传送、交互和方块放置 | 2.10.23 | 已修复 |
| BUG-044 | 2.10.23 | 2.10.24 | 正式开局时完整的队伍色皮革四件套没有得到强保证 | 开局沿用了复活时“仅当槽位为 `null` 才补齐”的逻辑，且未在开局物品完成后同步客户端；现在干净开局会一次性覆盖四个护甲槽并统一同步，复活仍保留永久护甲 | 2.10.24 | 已修复 |
| BUG-045 | 2.10.24 | 2.10.25 | 主大厅的“回到主大厅”红床点击后没有反应 | 物品点击被转换为需要玩家权限的 `/bw leave`，且非 Bungee 玩家已经在大厅世界时错误调用了代理切服路径；现在大厅红床直接执行内部返回流程，非 Bungee 模式始终传送到配置的大厅位置并重新应用大厅状态 | 2.10.25 | 已修复 |
| BUG-046 | 2.10.24 | 2.10.25 | 游戏玩家名虽然有队伍颜色，但名字前仍重复显示队伍名称 | 玩家名颜色和文字前缀同时携带了队伍信息；现在保留记分板队伍颜色与排序，只清空有队伍归属玩家的文字前缀 | 2.10.25 | 已修复 |
| BUG-047 | 2.10.25 | 2.10.26 | 创建竞技场时自动建队不能可靠地区分相近羊毛颜色，孤立羊毛也可能被识别成队伍 | 扫描结果通过颜色名称字符串中转，且邻近计数循环始终重复读取中心方块；现在使用羊毛 `Material` 到 `TeamColor` 的精确一对一映射，并真实要求附近至少 5 块同色羊毛 | 2.10.26 | 已修复 |
| BUG-048 | 2.10.26 | 2.10.27 | 旧自动生成器逻辑没有接入设置流程且不能严格验证三层结构，高级模式也会意外自动改写床位 | 旧逻辑从未被调用，只比较外围材质并通过传送玩家执行命令；现在快速模式保存时直接验证完整 `3×3×3` 结构并合并配置，高级模式只校验手动床位而不自动修改 | 2.10.27 | 已修复 |
| BUG-049 | 2.10.27 | 2.10.28 | 玩家可以在没有 `bw.shout` 时通过 `!消息` 绕过权限进行全场喊话 | `/shout` 有独立权限检查，但聊天格式化分支只执行冷却和收件人处理；现在两个入口共用 `Permissions.hasShoutPermission`，未授权消息会在扩大收件人前取消 | 2.10.28 | 已修复 |
| BUG-050 | 2.10.27 | 2.10.28 | 基础玩家命令、启用竞技场和调试开局的权限行为与官方权限表不一致 | 细粒度权限改造把官方无权限命令全部锁住，沿用了源码中的 `bw.enableRotation`，并允许权限节点代替调试开局 OP 身份；现在恢复官方默认行为和 `bw.enable`，仅将细粒度及旧节点保留为兼容入口 | 2.10.28 | 已修复 |
| BUG-051 | 2.10.27 | 2.10.29 | 全局钻石/绿宝石生成器在保存竞技场时隐式扫描，且只接受 `3×3` 实心资源底座 | 扫描调用被直接写入 `/bw save`，结构校验逐格要求底层等于资源块；现在改由快速设置指令 `/bw autoDetectGenerators` 显式触发，并接受实心或仅中心资源块两种底层 | 2.10.29 | 已修复 |
| BUG-052 | 2.10.25 | 2.10.30 | 主大厅“回到主大厅”红床只传送到本服大厅坐标，玩家点击后看起来没有作用 | 2.10.25 把大厅物品硬编码为内部大厅返回流程，没有区分代理主大厅；现在大厅专用分支直接向 `lobbyServer` 发送 BungeeCord `Connect` 数据包，其他阶段的离开物品保持原行为 | 2.10.30 | 已修复 |
| BUG-053 | 2.10.29 | 2.10.30 | 新版客户端可通过 Locator Bar 暴露玩家位置 | 竞技场默认规则和世界加载流程没有处理 `minecraft:locator_bar`；现在迁移所有竞技场规则，并在插件启动、世界加载、设置会话和竞技场初始化后强制关闭 | 2.10.30 | 已修复 |
| BUG-054 | 2.10.18 | 2.10.31 | 玩家从大厅进入竞技场设置或其他世界后，再次返回大厅可能没有进入提示 | 大厅提示使用去重集合，但原实现只在进入竞技场时清理；现在设置会话与所有世界切换都会同步大厅在线状态，返回时可再次播报且不重复 | 2.10.31 | 已修复 |
| BUG-055 | 2.10.14 | 2.10.32 | 玩家在已开局的竞技场中途加入旁观时，加入通知仍会发给游戏内和大厅玩家 | 旁观加入路径无条件复用了竞技场加入广播；现在统一在广播入口校验状态，只允许 `waiting` 和 `starting` 准备阶段 | 2.10.32 | 已修复 |
| BUG-056 | 2.10.32 | 2.10.33 | 开局时从未分配玩家的空队伍仍在侧边栏显示为已淘汰 | 侧边栏直接遍历全部配置队伍，无法区分“开局为空”和“参战后被淘汰”；现在在进入 `playing` 时快照实际参战队伍 | 2.10.33 | 已修复 |
| BUG-057 | 2.10.32 | 2.10.33 | 已淘汰玩家的 TAB 标记位于名字后面，颜色与括号也不统一 | 渲染代码忽略了已有的淘汰前缀配置，只使用后缀；现在前缀为 `&7[&c已淘汰&7] `，名字由原队伍颜色渲染 | 2.10.33 | 已修复 |
| BUG-058 | 2.10.32 | 2.10.33 | 旁观玩家在 TAB 或头顶仍显示生命值 | 旁观视角会主动给所有旁观者写入分数，且存活时留下的分数没有清理；现在旁观转换、事件刷新和周期刷新均强制清除 | 2.10.33 | 已修复 |
| BUG-059 | 2.10.30 | 2.10.33 | 玩家退出后的延迟侧边栏任务抛出 `Trying to get stats data of an unloaded player!` | 统计监听器在退出事件中卸载缓存，侧边栏仍使用会抛异常的严格读取；现在使用可空读取并拒绝为离线玩家刷新 | 2.10.33 | 已修复 |
| BUG-060 | 2.10.30 | 2.10.34 | 主大厅“回到主大厅”红床在代理配置不完整时仍然表现为点击无反应 | `Connect` 子通道按官方协议没有回执，旧实现发送后就视为成功；现在先查询当前节点和服务器列表，并针对直连后端、Velocity 兼容通道关闭、服务器名无效及目标为当前节点给出中文诊断 | 2.10.34 | 已修复 |
| BUG-061 | 2.10.21 | 2.10.35 | 执行 `/bw start debug` 后全服开始输出竞技场和逐生成器 `DEBUG:` 日志 | 单队测试参数直接把全局日志字段设为 `true` 且从不恢复，竞技场与生成器构造又记录低价值逐对象信息；现在测试开局不再修改日志状态，并删除两类启动刷屏 | 2.10.35 | 已修复 |
| BUG-062 | 2.10.30 | 2.10.36 | 返回红床仍可能点击无反应或走错返回路径 | 三个阶段的返回物品只保存相同命令，点击时依赖世界识别和命令文本猜测目标，交互事件还固定读取主手；现在物品持久标记明确区分代理大厅与本服大厅，并读取实际触发交互的手 | 2.10.36 | 已修复 |
| BUG-063 | 2.10.31 | 2.10.37 | 玩家加入/退出游戏及加入准备阶段竞技场的提示在主大厅不可见 | 插件加载时通过 `Location#getWorld()`获取大厅世界名，独立大厅尚未加载便永久缓存为空，所有大厅受众检查均失败；现在直接解析 `lobbyLoc` 中的世界名，并在竞技场广播前先写入玩家映射 | 2.10.37 | 已修复 |
| BUG-064 | 2.10.0 | 2.10.38 | 游戏结束时仍在竞技场世界的玩家被以 `You're not supposed to be here.` 踢出服务器 | `removePlayer` 发起 Paper 异步传送后，同一 tick 立即执行世界卸载前的硬编码踢人循环；现在等待全部玩家离开和传送完成后才恢复地图，失败时保留世界并重试 | 2.10.38 | 已修复 |
| BUG-065 | 2.10.38 | 2.10.39 | 火球无法炸毁着火表面的玩家放置方块，旧竞技场仍可能发生火势蔓延 | 爆炸射线把 FIRE/SOUL_FIRE 当成未追踪地图方块；同时 Paper 1.21.11 已移除旧 `doFireTick` 规则。现在火焰对射线透明，拦截 `SPREAD` 点燃，并自动迁移到 `fireSpreadRadiusAroundPlayer:0` | 2.10.39 | 已修复 |
| BUG-066 | 2.10.39 | 2.10.40 | 商店中的袖珍弹出塔显示红色 `Name not set`，没有中文名称和说明 | `shop.yml` 使用稳定 ID `tower`，三份内置语言却误用显示文本 `Compact Pop-up Tower` 作为内容 ID；现在统一为 `tower`，自动迁移旧名称/Lore 并清理系统占位值 | 2.10.40 | 已修复 |
| BUG-067 | 2.10.40 | 2.10.41 | 多人容量地图中，开局实际只有一人的队伍仍被限制在队内私聊 | 聊天路由错误判断竞技场配置的 `maxInTeam`，而不是本局实际分队结果；现在快照每队开局人数，单人队普通聊天直接进入竞技场公屏 | 2.10.41 | 已修复 |
| BUG-068 | 2.10.42 开发版 | 2.10.42 | 固定半身护甲后，自定义的纯上半身商品可能扣除货币却不发放物品 | 最初只在发放阶段拒绝上半身护甲，交易已经完成扣款；现在上半身条目不再载入，且空商品层会在扣款前被拒绝 | 2.10.42 | 已修复 |
| BUG-069 | 2.10.42 | 2.10.43 | 竞技场关闭昼夜循环后仍可能停留在地图保存时的任意时间，旧配置还可用 `doDaylightCycle:true` 重新开启循环 | 初始化只应用配置规则，没有校准世界时间，迁移也只在缺失规则时补默认值；现在加载和设置阶段固定为 1000 tick，并强制规范昼夜规则为 false | 2.10.43 | 已修复 |
| BUG-070 | 2.10.43 | 2.10.44 | 快速设置中看不到最大队伍人数入口，创建第 4 支队伍时还会把容量覆盖为 2，间接限制最少人数只能选 1/2 | `setMaxInTeam` 只在高级列表展示，`CreateTeam` 又按队伍数重写容量；现在所有模式展示入口，创建队伍不再修改容量，最少人数补全读取当前最大值 | 2.10.44 | 已修复 |
| BUG-071 | 2.10.43 | 2.10.44 | 玩家和 NPC 朝向保存任意 yaw/pitch，旧大厅与独立 Facing 字段也不会统一 | 通用坐标和竞技场 Facing 使用不同规范化路径，pitch 只限制范围；现在 API 与插件共用 90 度 yaw 吸附、pitch 归零规则，并迁移全部已知持久化位置 | 2.10.44 | 已修复 |
| BUG-072 | 2.10.43 | 2.10.44 | Locator Bar 只在插件构造、世界加载和竞技场初始化关闭，没有覆盖世界初始化阶段 | 部分世界管理器可能在初始化与加载之间应用规则；现在额外监听每次 `WorldInitEvent`，并保留后续阶段的重复强制关闭 | 2.10.44 | 已修复 |
| BUG-073 | 2.10.44 | 2.10.45 | 服务器卡顿时已成功放置的方块可能在放置者客户端保持为空气，点击对应位置后才重新显示 | 服务端已保存方块和所有权，但客户端放置确认可能延迟；现在按玩家、按 tick 去重成功放置坐标，并在下一 tick 用真实状态批量重同步，不发送整份背包 | 2.10.45 | 已修复 |
| BUG-074 | 2.10.34 | 2.10.46 | 返回代理大厅前向玩家显示代理检查、节点、服务器列表、连接进度和失败诊断，而不是直接切服 | 旧流程先发送 `GetServer`/`GetServers` 并等待回包；现在点击后只立即发送 `Connect <lobbyServer>`，删除查询监听与超时任务，插件侧可检测的失败只写服务端日志 | 2.10.46 | 已修复 |
| BUG-075 | 2.10.47 | 2.10.48 | 新版客户端每隔约 60 秒可能因 `Cannot remove from team 'bw_t_*'` 网络协议错误断线 | TAB 状态变化会删除并重建玩家队伍，队伍名还使用可碰撞的 `String.hashCode()`；周期任务又无条件重写相同元数据。现在玩家 UUID 对应稳定短队伍名，状态原地增量更新，未变化刷新不再写包 | 2.10.48 | 已修复 |
| BUG-076 | 2.10.48 | 2.10.49 | 关闭昼夜循环后竞技场仍会保留保存时间，树叶也可能继续腐烂 | 只在载入时设置时间且未关闭随机方块刻；现在持续锁定正午 6000 tick、晴天和 randomTickSpeed 0，并拦截残留自然方块事件 | 2.10.49 | 已修复 |
| BUG-077 | 2.10.48 | 2.10.49 | 商店和升级村民关闭 AI 后仍可能被外部作用移动或改变头部方向 | 仅在生成瞬间设置 AI 和 rotation；现在同时关闭感知、重力，并只在检测到偏移时恢复固定位置、身体和视线方向 | 2.10.49 | 已修复 |
| BUG-078 | 2.10.49 | 2.10.50 | 旧配置自动升级时，新增键统一出现在 YAML 末尾，而不是与新版中的相关键放在一起 | Bukkit 的 `copyDefaults(true)` 会按插入顺序把缺失默认值追加到末尾；现在迁移器按新版默认树递归排序，并保留自定义键、自定义值和注释 | 2.10.50 | 已修复 |
| BUG-079 | 2.10.49 | 2.10.51 | 竞技场时间不是 6000 tick 时，每秒守卫任务反复抛出 `BootstrapMethodError: bootstrap method initialization exception` | `World#setTime` 在真正更新时间前发布 `TimeSkipEvent`，旧监听器取消事件后立即再次调用 `setTime`，形成事件递归；现在仅放行到达 6000 tick 的跳跃，事件内不再修改时间 | 2.10.51 | 已修复 |
| BUG-080 | 2.10.51 | 2.10.52 | 为了锁定竞技场环境，插件每秒遍历全部地图并重复检查时间、天气和多条游戏规则，且没有独立阻止雷暴开始 | 删除周期扫描，改为初始化时设置一次游戏规则，之后用时间、下雨和雷暴事件阻止状态变化；固定时间按需求改为 12000 tick | 2.10.52 | 已修复 |
| BUG-081 | 2.10.52 | 2.10.53 | 把 `12000 tick` 当作原版正午，导致竞技场实际固定在接近日落的时刻 | 原版命令参数是 `noon` 而不是 `minecraft:noon`；Bukkit 时间 API 只接收 long，因此直接使用 `/time set noon` 等价的 6000 tick，避免命令解析开销 | 2.10.53 | 已修复 |
| BUG-082 | 2.10.49 | 2.10.54 | 竞技场载入和配置迁移反复把 `minInTeam` 覆盖为 `maxInTeam`，开局资格还要求整个竞技场满员并错误校验未启用空队 | 只允许 setup 设置操作自动同步下限；已有配置保持原值。匹配在至少两支启用队伍达到下限时开始，最终验证忽略本局未选中的空队 | 2.10.54 | 已修复 |
| BUG-083 | 2.10.56 | 2.11.0 | 游戏内 TAB 玩家名称无法稳定显示队伍颜色，头顶名称也可能保持白色 | 客户端收到非空自定义 `playerListName` 时不会套用计分板队伍格式；现在在 BedWars TAB 上下文存活期间引用计数式清空该组件，并在全部查看上下文结束后安全恢复，计分板颜色同时作用于 TAB 和头顶名称 | 2.11.0 | 已修复 |
| BUG-084 | 2.10.56 | 2.11.0 | 普通玩家无法使用 `/shout` 和 `!消息` | `bw.shout` 在插件清单中被声明为默认不授予；现在默认值改为 `true`，仍允许权限插件显式撤销，也不改变喊话冷却绕过权限 | 2.11.0 | 已修复 |
| BUG-085 | 2.10.56 | 2.11.0 | `/bw save` 会放行缺少队伍铁或金生成器的竞技场 | 保存流程只验证床位，没有验证基础经济来源；现在按队伍列出缺失的铁、金生成器并阻止保存，补齐后才能结束设置 | 2.11.0 | 已修复 |
| BUG-086 | 2.11.0 | 2.12.0 | `/bw gui` 默认只显示 7 张竞技场，地图更多时没有任何方式浏览剩余竞技场 | 选择器固定使用 27 格和单页槽位；现在默认扩展为 54 格、每页 45 张地图，并提供保留分组条件的上一页/下一页导航 | 2.12.0 | 已修复 |
| BUG-087 | 2.11.0 | 2.12.0 | 有预组队时开局资格依赖一次随机贪心分配，可能错误停止倒计时，也可能强拆唯一小队凑出两个对手队 | 资格检查与最终分配改用保持小队边界的精确规划，优先启用最多合法队伍；只有超过单队上限的外部大队伍才允许拆分 | 2.12.0 | 已修复 |
| BUG-088 | 2.12.0 | 2.12.1 | 分队规划使用 `SearchState`/`failedStates` 记忆化状态表保存历史负载组合，不符合队伍数量应实时计算的要求 | 队伍数改用人数上下限公式直接计算，小队放置只读取当前负载和剩余人数；删除全部状态表及查表逻辑 | 2.12.1 | 已修复 |
| BUG-089 | 2.12.1 | 2.13.0 | 游戏内关闭右侧计分板时，TAB 名称和头顶名称都无法使用所属队伍颜色 | 玩家列表计分板上下文错误依赖游戏侧边栏开关；现在按各阶段 TAB 格式开关独立保留上下文，并继续由同一计分板队伍渲染两处颜色 | 2.13.0 | 已修复 |
| BUG-090 | 2.12.1 | 2.13.0 | `/bw team` 所谓选队 GUI 实际只能邀请固定队友，无法选择地图中的红队、蓝队等真实队伍 | 旧实现混淆“固定小队”和“游戏队伍”；现在新增竞技场本地预选状态与真实队伍 GUI，固定队友界面移至 `team squad` | 2.13.0 | 已修复 |
| BUG-091 | 2.12.1 | 2.13.0 | 旧服的锋利升级仍可能只卖到较低等级，无法稳定购买至锋利 IV | 新层级主要停留在运行时默认值，且载入依赖 YAML 键顺序；现在通过架构 7 显式持久化缺失层级并按层级数字排序 | 2.13.0 | 已修复 |
| BUG-092 | 2.12.1 | 2.13.0 | 普通玩家仍可能无法执行 `/shout` | 命令未在 `plugin.yml` 声明，动态注册遇到同名命令时会跳过，旧 `allowed-commands` 还可能二次拦截；现在声明并绑定插件命令，且内置喊话不依赖旧允许列表 | 2.13.0 | 已修复 |
| BUG-093 | 2.12.1 | 2.13.0 | `/bw save` 存在床位错误时不会提示同一竞技场中缺少铁或金生成器的队伍 | 床位校验分支提前返回；现在先收集两类错误、全部提示后再统一阻止保存 | 2.13.0 | 已修复 |
| BUG-094 | 2.12.1 | 2.13.0 | `/bw gui` 在旧版或较小自定义布局中仍可能看不到全部竞技场，翻页按钮也可能失效 | 运行时没有为容量不足布局扩容，导航又依赖物品自定义数据；现在自动扩展至 54 格并由 GUI 持有者直接记录翻页槽位 | 2.13.0 | 已修复 |
| BUG-095 | 2.12.1 | 2.13.0 | 开局资格、固定小队和真实选队没有形成同一条最终分配链，边界组合下行为与玩家预期不一致 | 资格检查和实际分配现在共同使用实时人数公式与不可拆小队规划，最大化合法活跃队伍数；真实选队只在不破坏 `minInTeam..maxInTeam` 时生效 | 2.13.0 | 已修复 |

## 新 BUG 记录模板

提交新问题时，请复制下面的模板：

```text
编号：BUG-待分配
状态：待确认
发现版本：例如 2.8.0
服务端：Paper 完整版本和构建号
Java：完整版本
问题描述：
复现步骤：
1.
2.
3.
预期行为：
实际行为：
发生频率：必现 / 偶发 / 仅一次
完整日志或 mclo.gs：
相关竞技场配置：
是否可以在无其他插件环境复现：
```

## 处理规则

1. 日志必须保留完整异常堆栈；只有异常标题而没有调用位置时先标记为 `待确认`。
2. 优先使用与生产环境一致的 Paper 1.21.11 和 Java 21 复现。
3. 修复后至少执行 `mvn -B -ntp clean verify`。
4. 发布版本遵循语义化版本：兼容修复递增 `PATCH`，兼容新功能递增 `MINOR`，不兼容变化递增 `MAJOR`。
5. 仅修改文档或测试且不发布新 JAR 时不修改版本号；只要发布新 JAR，就必须使用尚未发布的版本号。
6. 发布后在本表填写修复版本、提交地址和验证结果。
